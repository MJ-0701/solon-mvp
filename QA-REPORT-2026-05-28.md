---
doc_id: solon-qa-report-2026-05-28
title: "Solon Product QA/QC 전수검사 — 2026-05-28"
visibility: oss-public
doc_type: qa-report
language: ko
updated: 2026-05-28
summary: "0.6.141 시점 codex 인계 이후 distribution repo 의 빵꾸/하네스 누락 전수검사 결과."
load_when: "Read when reviewing 0.6.x maintenance gaps or planning a Gate 6 cleanup sprint."
---

# Solon Product QA/QC 전수검사 — 2026-05-28

- **대상**: `/Users/mj/tmp/solon-product` (VERSION 0.6.141, main, codex 인계 + 채명정 머지 트레인)
- **세션**: Cowork `clever-optimistic-mayer`
- **샌드박스 한계**: per-test 30s+ 가 필요한 review/loop 통합 테스트는 timeout 으로 표시. 본 환경에서 git push 불가 (commit 까지만 가능).

## 0. 한줄 요약

테스트 110건 중 **107 PASS / 3 TIMEOUT** (lens-aliases, plan-review-preflight 는 11s 내 실제 통과, 단 **`test-review-auth-preflight` 은 40s 에도 hang** — 0.6.139 thin-router 리팩토링 회귀 의심). 하네스 자체는 거의 다 조여있으나, **`scripts/sfs-harness.sh` 가 private dev staging path (`2026-04-19-sfs-v0.4/...`) 를 코드 안에 박아 넣어 CLAUDE.md 절대금지 룰 위반**, 그리고 `test-private-dev-path-hygiene.sh` 가 이 패턴을 잡지 못해 검출이 새는 것이 최우선 수정 포인트.

## 1. SEV-1 (즉시 수정)

### 1.1 `scripts/sfs-harness.sh` 가 사용자 개인 docset 경로 leak

```
scripts/sfs-harness.sh:116:  [ -d "2026-04-19-sfs-v0.4/solon-mvp-dist/tests" ] && return 0
scripts/sfs-harness.sh:127:  [ -f "2026-04-19-sfs-v0.4/scripts/verify-product-release.sh" ] && return 0
```

- **위반 룰**: `solon-product/CLAUDE.md` §"절대 금지" — *"Active 제품 파일에는 private dev staging checkout 이름이나 절대경로를 쓰지 않는다."*
- **현재 우회 채널**: `test-private-dev-path-hygiene.sh` 가 `agent_architect`, `/Users/mj/agent_architect` 만 patterns 로 사용. dated docset 디렉토리명 (`2026-04-19-sfs-v0.4`) 은 사전 정의된 패턴에 들어가 있지 않아 통과해버림.
- **조치**:
  1. `scripts/sfs-harness.sh` 의 `detect_test_surface` / `detect_release_surface` 에서 dated docset 경로를 제거. 외부 fixture 라면 환경변수 (`SFS_HARNESS_EXT_TEST_DIR` 등) 로 받아오는 방식으로 우회.
  2. `test-private-dev-path-hygiene.sh` patterns 에 `YYYY-MM-DD-sfs-v\d` 정규식 / `solon-mvp-dist` / `phase1-mvp-templates` 추가해 회귀를 잡는다.
- **CHANGELOG / RELEASE-NOTES 의 historical 언급은 허용**(룰 명시) — 이번 노출은 active 코드 패스라 명백한 위반.

### 1.2 `test-review-auth-preflight.sh` 가 hang (40s timeout)

- **현상**: `sfs init --layout thin --yes` 후 두 번째 `sfs review --gate 3 --executor gemini` (auth 통과 경로) 호출이 stdin 을 close 하지 않아 fake gemini binary (`while IFS= read -r _line; do :; done`) 가 EOF 대기로 멈춤.
- **회귀 가설**: 0.6.139 release (thin SFS.md router + 대형 bin/sfs 리팩토링) 시점에 `templates/.sfs-local-template/scripts/sfs-review.sh` 또는 dispatch 의 stdin handling 이 바뀌면서 review executor 에 EOF 가 전달되지 않게 된 것으로 추정. 같은 테스트의 첫 번째 `--no-auth-interactive` 경로는 즉시 exit 9 로 끝나므로 stdin 닫힘 문제 없음. authenticated 분기에서만 발생.
- **영향**: production 사용자 흐름에서도 review executor 가 stdin 으로 입력을 기다리는 모델 (특히 codex/gemini CLI bridge) 일 때 동일 hang 발생 가능. **사용자 보고 없는 silent hang 위험**.
- **조치**:
  1. `sfs-review.sh` 의 executor 호출 직전 `< /dev/null` 명시 또는 `cmd < <(:)` 로 stdin 차단.
  2. test 의 fake gemini 가 stdin 을 무한 read 하는 패턴 자체를 유지하면서 실제 코드 경로가 EOF/close 를 보장하는지 정적으로 점검하는 가드 (`grep -F '</dev/null'` 같은) 를 별도 테스트로 추가.

## 2. SEV-2 (Gate 6 / 곧 빵꾸)

### 2.1 `test-private-dev-path-hygiene.sh` 가 좁다

- §1.1 과 한 묶음. 현재 patterns 가 `agent_architect` 와 그 경로 변형만 봄. dated docset 디렉토리 / docset 내부 fixture 이름 / 사용자 home 변형이 들어와도 잡히지 않음. 본 세션에서 그것이 실제로 발생함을 확인.
- 추가로 `SED_INPLACE`, fake bin paths 등에서 `/sessions/clever-optimistic-mayer/...` 같은 cowork sandbox 경로가 임시로 들어오는지도 (테스트만, active 코드는 절대 아님) 점검 권장.

### 2.2 `templates/.sfs-local-template/divisions.yaml` placeholder 미치환

- 파일은 `<PROJECT-NAME>` (line 16) 와 `<DEPLOY>` (line 42) 를 포함.
- `install.sh:600~624` 의 divisions.yaml 처리 블록은 `<DATE>` 와 `<SOLON-VERSION>` 만 sed 치환. `<PROJECT-NAME>` 는 model-profiles.yaml 에서는 치환되는데 divisions.yaml 만 누락 — 일관성 깨짐.
- 결과: consumer `.sfs-local/divisions.yaml` 에 `project: "<PROJECT-NAME>"` 가 raw 로 남음. CLAUDE.md.template §"templates/ 변경 시 — 도메인 특화 제거" 의 정신과 모순.
- **조치**: install.sh divisions.yaml sed 호출에 `-e "s|<PROJECT-NAME>|$PROJECT_NAME|g"` 추가. 그리고 `<DEPLOY>` 는 안내만 출력하던 채널에서도 substitution 또는 명시적 manual hint 로 일관 처리.

### 2.3 HTML-first 정책 vs 실제 산출물 0건

- `solon-product/CLAUDE.md` §5: "실제 사용자·외부 독자·온보딩 대상이 읽는 설명서/가이드/보고서/핸드북/랜딩성 문서는 기본 산출물을 HTML 로 작성".
- 실측: `find docs -name '*.html' | wc -l` → 0, `find docs -name '*.md' | wc -l` → 104.
- `test-user-facing-docs-html-first.sh` 는 정책 문자열 ("User-facing docs HTML-first") 이 agent surface 8 곳에 존재하는지만 검사. 실제 HTML 산출물 존재나 빌드 파이프라인 유무는 검증하지 않음.
- **조치**: 정책 자체를 "HTML-encouraged, MD 허용" 으로 약화하거나, 핵심 user-facing 문서 (`README.md`, `GUIDE.md`, `BEGINNER-GUIDE.md`, `docs/{en,ko}/index.md`) 에 대해 HTML 산출물 또는 pandoc/markedjs 빌드 step 을 도입해 일치시킨다.

## 3. SEV-3 (관측 강화 / 가독성)

### 3.1 동기 테스트 harness 의 interactive prompt 통과 로그가 stdout 으로 누설

`run-all.sh` 가 `bash "$t"` 로 직접 실행 — `ASSUME_YES=1` 분기에서 `confirm` 이 `"$1 (y/N) [N]: y"` 를 stderr 로 출력하고 stderr 를 capture 하지 않아 test stdout 에 한국어 prompt 가 그대로 노출됨. 진행 상황 파악을 어렵게 함.

- **조치**: `run-all.sh` 가 각 test 의 stderr 를 임시 파일로 분리, FAIL 시에만 출력. 또는 install.sh 의 confirm 함수가 `ASSUME_YES=1` 일 때는 묵음.

### 3.2 unsafe `${arr[@]}` 확장 — bash 3.2 회귀 가능성

`test-nounset-empty-array-expansion.sh` 는 `bin/sfs` 의 `dep_args` 와 `sfs-loop.sh` 의 `extra_flags` 만 정적 체크. 다음 스크립트들도 `"${arr[@]}"` 패턴을 쓰지만 idiom (`${arr[@]+...}`) 으로 보호되어 있지 않다 (각 호출 직전 비어 있지 않음 검사가 들어가 있긴 하나 향후 리팩토링 회귀 위험):

```
scripts/sfs-archive-branch-sync.sh
scripts/sfs-measure.sh
scripts/sfs-bootstrap.sh
scripts/sfs-sprint-yml-validator.sh
scripts/sfs-migrate-artifacts.sh
scripts/sfs-storage-precommit.sh
```

- 모두 현재는 명시적 `(( ${#arr[@]} == 0 ))` 가드 뒤에 호출되므로 즉시 버그는 아님.
- **조치**: nounset 테스트의 정적 체크 범위를 distribution 의 모든 `scripts/*.sh` + `templates/.sfs-local-template/scripts/*.sh` 로 확장하고, `"${arr[@]}"` 사용지점마다 그 직전 30 라인 안에 empty 가드가 있는지 알고리즘적으로 확인하는 보강을 권장.

### 3.3 `test-sfs-implement-plan-review-preflight.sh` 의 install.sh prompt 7회 중복

테스트 trace 에서 `계속 진행할까요? (y/N) [N]: y` 가 7번 반복. ASSUME_YES 분기여서 동작 자체는 OK 지만, 같은 install.sh 가 한 테스트 안에서 7번 invoke 된다는 의미 = 통합 테스트 setup 이 무겁고, run-all.sh 총 소요를 늘리는 주범.

- **조치**: 테스트 setup 에서 `sfs init` 결과를 한 번 만들고 `cp -a` 로 복제하는 식으로 reuse 가능 여부 검토.

## 4. 통과 확인된 영역 (참고용)

| 영역 | 상태 |
| --- | --- |
| `set -euo pipefail` shebang 일관성 (install/upgrade/uninstall/bin/sfs) | OK |
| `--yes` flag 흐름 (install.sh ASSUME_YES + bin/sfs init 패스스루) | OK |
| 충돌 4-option (s/b/o/d) prompt | OK |
| 멱등성 마커 (`.gitignore` solon-product 블록, model-profiles.yaml/ divisions.yaml 의 "기존 유지" 분기) | OK |
| CHANGELOG vs VERSION sync (0.6.141 헤더 일치, [Unreleased] 빈 채로 유지) | OK |
| Gate 1~7 표기 일관성 (display label 함수가 G-1~G5 → Gate 1~7 매핑) | OK |
| 평문 secret / API key 누설 hunt | clean |
| `set -x` 등 디버그 활성 흔적 | clean |

## 5. 후속 작업 권고 순서

1. **SEV-1 두 건을 한 sprint 로 묶어 release: 0.6.142** — harness path leak 제거 + review-auth-preflight stdin 차단.
2. hygiene 테스트 패턴 보강 (정규식 docset, sandbox path negative) 을 같은 sprint 의 retro evidence 로 남긴다.
3. SEV-2 divisions.yaml placeholder 와 HTML-first 정책-실태 격차는 별도 토론 (정책 약화 vs 산출 강화) 후 결정.
4. SEV-3 들은 다음 짧은 cleanup window 에 묶어 처리.

## 부록 A. 테스트 실행 결과 raw

```
PASS:    107
TIMEOUT:   3
   test-review-auth-preflight.sh        (실제 hang, §1.2 참고)
   test-review-lens-aliases.sh          (8s 초과, 11s 안에 PASS — 본 환경 한계)
   test-sfs-implement-plan-review-preflight.sh  (8s 초과, 11s 안에 PASS)
```

## 부록 B. 검토 메서드

- 110개 `tests/test-*.sh` 를 per-test 8s timeout 으로 순차 실행 (cowork bash 콜 45s 한도 회피).
- 8s timeout 적중분은 35~60s 한도로 재실행해 실제 hang/PASS 구분.
- `git log -30`, `git show --stat` 으로 0.6.112~0.6.141 변경 영역 파악, 0.6.139 (thin router 대규모 리팩토링) 와 0.6.141 (harness command 도입) 을 회귀 가설 후보로 선정.
- private dev path / hardcoded secret / debug 활성 / 빈 fixture / placeholder sync / Gate 표기 / HTML-first 정책 등 7개 정적 cross-cut grep 패스.
