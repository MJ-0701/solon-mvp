---
wu_id: WU-21
title: "Phase 1 킥오프 dry-run (D-2) — install.sh sandbox + setup-w0.sh pre-flight"
status: done
opened_at: 2026-04-25T09:15:00+09:00
closed_at: 2026-04-25T09:45:00+09:00
session_opened: confident-loving-ride
session_closed: confident-loving-ride
final_sha: TBD_18TH_COMMIT   # 본 WU-21 commit sha — 다음 세션이 git log 로 확인 후 backfill (P-03 예방)
refresh_wu: null
visibility: raw-internal
entry_point: "sandbox /tmp/wu21-dry/ 에서 solon-mvp/install.sh 를 가짜 consumer 에 주입 + setup-w0.sh pre-flight 시뮬. 발견 이슈 본 파일 §Findings 에 누적."
depends_on_reads:
  - PHASE1-MVP-QUICK-START.md §2
  - ~/workspace/solon-mvp/install.sh (v0.2.4-mvp)
  - phase1-mvp-templates/setup-w0.sh
  - phase1-mvp-templates/verify-w0.sh
files_touched:
  - sprints/WU-21.md (본 파일)
  - PROGRESS.md (mutex claim + snapshot)
  - (없음, dry-run 은 sandbox /tmp/ 한정 — solon-mvp/ 는 read-only)
decision_points: []
learning_patterns_emitted: []
sub_steps_split: false
---

# WU-21 — Phase 1 킥오프 dry-run (D-2)

> **목적**: 2026-04-27 (월) Phase 1 MVP 킥오프 D-day 이전 **D-2** 시점에서 주력 스크립트 2개의 행동을 sandbox 에서 검증, 발견 이슈를 미리 처리해 D-day 사용자 Mac 실행에 5 분 exit 보장.
> **사용자 지시**: resume_hint default_action (a) — "a로 ㄱㄱ" (2026-04-25 09:14 KST).
> **범위**: install.sh 완전 dry-run + setup-w0.sh pre-flight (clone 단계 skip). sandbox = /tmp/wu21-dry/.

## 전제 / 경계

- Claude 세션 sandbox (/tmp/) 에서만 실행. 사용자 `~/workspace` · GitHub · 네트워크 건드리지 않음.
- `solon-mvp/` (v0.2.4-mvp stable) 는 read-only 참조. 수정 시 §1.13 R-D1 위반.
- 발견된 버그/이슈는 본 파일 §Findings 에 누적 → 해결은 다음 WU (WU-21.1 / WU-22) 로 분리.

## Findings (dry-run 실행 결과)

### F-01 · install.sh v0.2.4-mvp sandbox — FULL PASS
- sandbox: `/tmp/wu21-dry/fake-consumer/` (git init + pre-existing README + .gitignore 로 충돌 시나리오 포함).
- 실행: `bash ~/workspace/solon-mvp/install.sh --yes` → **exit 0**.
- 설치물 확인: `SFS.md` · `CLAUDE.md` · `AGENTS.md` · `GEMINI.md` · `.claude/commands/sfs.md` · `.sfs-local/{VERSION, divisions.yaml, events.jsonl, sprints/.gitkeep, decisions/.gitkeep}`.
- 자동 치환 동작: `<DATE>=2026-04-25`, `<SOLON-VERSION>=0.2.4-mvp`.
- `.gitignore` marker-based merge 정상 (pre-existing `node_modules/` 보존 + `### BEGIN/END solon-mvp ###` 블록 append).
- **멱등성 OK**: 재실행 → exit 0, 기존 파일 파괴 없음.
- IP 경계 (install.sh 기준 = OSS 제품명 "Solon" 은 허용, 내부 docset 경로만 금지): `agent_architect` · `sfs-v0.4` · `solon-docset` · `2026-04-19` 전부 **0 건**. 제품명 "Solon" 언급 2건 (CLAUDE.md.template 1/3 줄) 은 **의도된 배포물 문구** (R-D1 § OSS public 정의 허용 범위).

### F-02 · setup-w0.sh pre-flight + 시뮬레이션 — PASS
- env 검증 동작 확인: `PROJECT_NAME`/`SOLON_DOCSET`/`WORKSPACE` 누락 시 line 10/11/12 에서 즉시 exit (`${VAR:?msg}` 패턴).
- `TEMPLATES_DIR` 오탐 케이스 (잘못된 SOLON_DOCSET): "❌ TEMPLATES_DIR 없음" 명시적 exit 1 (정상).
- 의존 파일 전수 확인: `.gitignore.snippet` ✅ · `.sfs-local-template/divisions.yaml.template` ✅ · `CLAUDE.md.template` ✅ · `README.md.template` ✅.
- clone 단계만 skip 하고 Step 4-8 수동 재현 (sandbox `/tmp/wu21-dry/admin-panel-mvp/`) → 3 commits 정상 생성, IP 경계 clean, verify-w0.sh **exit 0**.

### F-03 · verify-w0.sh 결과 (setup-w0.sh 출력 대상) — conditional pass
- PASS 11 · FAIL 0 · WARN 3 (placeholder 잔여 — Stack/DB/DEPLOY/UI-LIB/AUTH/RECEIPT-API/LICENSE-OR-IP-NOTICE/EMAIL).
- 모든 WARN 은 **사용자가 에디터에서 Stack 결정 후 수동 치환** 대상 (QUICK-START.md §1 D3 결정 대기). **설계된 동작, 버그 아님**.
- D-day 에 Stack 결정하고 치환 후 재실행하면 exit 0 + WARN 0 예상.

### F-04 · verify-w0.sh false-positive 2건 (design 이슈, D-day 차단 X)

**(a) Check #7 — install.sh output 에 대해 over-strict**:
- install.sh 가 만든 CLAUDE.md 에 대해 verify-w0.sh 를 실행하면 Check #7 이 "Solon 키워드 = IP leak" 으로 판정하여 **FAIL**.
- 원인: verify-w0.sh 의 regex `grep -iE "solon|agent_architect|sfs-v0\.4"` 이 OSS 제품명 "Solon" 까지 차단. 하지만 install.sh 는 OSS 배포물이라 "Solon" 이라는 제품명 기재가 정당 (adapter 문구 `# CLAUDE.md — Claude Code adapter for Solon SFS`).
- 처리: **verify-w0.sh 는 phase1-mvp-templates (setup-w0.sh 전용) 검증 도구로 명시**. install.sh 출력은 다른 검증 기준 필요. 향후 WU 로 분리 (sf-verify-install.sh or verify-w0.sh 에 모드 플래그 `--target=install|setup-w0`).

**(b) Check #6 — 설명용 placeholder 를 치환 대상으로 오탐**:
- `<N>-<short-title>.md`, `<YYYY-W>-sprint-<N>/`, `<N>-activate-<division>.md` 같은 문서 내 **형식 설명 placeholder** 까지 grep 에 걸림.
- 사용자가 치환해야 할 진짜 placeholder (`<STACK>`, `<DB>` 등) 과 섞여서 표시됨 → 노이즈.
- 처리: 후속 WU 로 grep 정규식 튜닝 or 설명용 marker 분리 (예: `{{PROJECT-NAME}}` vs 설명용 `<...>`).

## Result / Exit

- **install.sh sandbox**: **PASS** (exit 0, 멱등성 OK, IP 경계 OK).
- **setup-w0.sh pre-flight + 시뮬**: **PASS** (env 검증 동작, 3 commits 생성, verify-w0.sh exit 0).
- **verify-w0.sh (setup-w0.sh 출력 대상)**: **exit 0 + WARN 3** (Stack 치환 대기 — 정상).
- **D-day (2026-04-27 월) 실행 차단 요소**: **없음**. 사용자 Mac 에서 QUICK-START.md §2 runbook 그대로 실행 가능.
- **후속 TODO 2건** (F-04): verify-w0.sh check #7 mode flag + check #6 placeholder 정규식 튜닝 — D-day 이후 WU 로 분리.

## 학습 포인트

- **두 스크립트 역할 분리** (WU-21 최초 명시): `install.sh` = OSS 배포 adapter 주입 / `setup-w0.sh` = 관리자페이지 MVP greenfield 부트스트랩. 템플릿 2 set 완전 분리 (phase1-mvp-templates vs solon-mvp/templates). verify-w0.sh 는 **setup-w0.sh 출력 전용**.
- **sandbox dry-run 유효성** 확인 — clone 단계만 우회하면 `/tmp` 에서 완전 재현 가능. D-day 전 주말 dry-run 패턴 반복 가능.
