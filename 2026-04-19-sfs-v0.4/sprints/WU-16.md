---
wu_id: WU-16
title: "기존 WU (WU-7 ~ WU-14.1) 이관 — WORK-LOG entry → sprints/WU-<id>.md 독립 파일 + sessions/ 3-part retrospective 생성"
status: done
opened_at: 2026-04-24T09:45:00+09:00
closed_at: 2026-04-24T10:15:00+09:00
session_opened: brave-hopeful-euler
session_closed: brave-hopeful-euler
final_sha: 2b8b69e        # WU-16.1 에서 backfill 완료
refresh_wu: WU-16.1       # forward sha backfill 전용
visibility: raw-internal  # 운영 infra 규율 — OSS fork 비포함
entry_point: |
  PROGRESS.md `② In-Progress` → step-<n> 위치에서 재개.
  본 WU 는 "기존 WU 이관" — WORK-LOG 의 append-style entry 8건을 독립 파일로 분리.
  본문 내용 자체 변형 없음 (minimal cleanup, 원칙 2 준수).
depends_on_reads:
  - CLAUDE.md §5           # WU frontmatter 스키마
  - CLAUDE.md §7           # Visibility tier 규율
  - WORK-LOG.md L424-L623  # WU-7/7.1/10/10.1/13/13.1/14/14.1 entries
  - sprints/WU-15.md §4    # Out-of-scope 명시된 WU-16 범위
  - sessions/_INDEX.md     # 과거 3 세션 placeholder
  - NEXT-SESSION-BRIEFING.md §8  # 최근 세션 요약 (retrospective 재구성 재료)
files_touched:
  # 신규 (sprints/) ──────────────────────
  - sprints/WU-7.md                    # 신규 — 이관
  - sprints/WU-7.1.md                  # 신규 — 이관
  - sprints/WU-10.md                   # 신규 — 이관
  - sprints/WU-10.1.md                 # 신규 — 이관
  - sprints/WU-13.md                   # 신규 — 이관
  - sprints/WU-13.1.md                 # 신규 — 이관
  - sprints/WU-14.md                   # 신규 — 이관
  - sprints/WU-14.1.md                 # 신규 — 이관
  - sprints/WU-16.md                   # 본 파일
  # 신규 (sessions/) ──────────────────────
  - sessions/2026-04-20-funny-compassionate-wright.md   # 신규 — 3-part retrospective
  - sessions/2026-04-21-serene-fervent-wozniak.md       # 신규 — 3-part retrospective
  - sessions/2026-04-21-relaxed-vibrant-albattani.md    # 신규 — 3-part retrospective
  # 수정 ────────────────────────────────
  - sprints/_INDEX.md                  # "Backfill 대상" → "v2 네이티브" 행 이동 + WU-13/13.1 누락 추가
  - sessions/_INDEX.md                 # 과거 3 세션 placeholder → 실제 파일 참조
  - PROGRESS.md                        # frontmatter (current_wu=WU-16 · mutex claim) + 본문 덮어쓰기
  - .gitignore                         # 루트 — bkit plugin 메모리 차단 (docs/.bkit-memory.json 외)
decision_points: []        # 본 WU 는 이관 작업, 의미 결정 없음 (원칙 2 준수). 범위 해석만 사용자 암묵 승인 (Option β minimal).
learning_patterns_emitted: []   # 패턴 실체화는 WU-18 에서 수행
sub_steps_split: false      # 200 line 이내 예상. 초과 시 split.
scope_interpretation: |
  sprints/WU-15.md §4 Out-of-scope "WU-7 ~ WU-14.1" 을 Option β (minimal) 로 해석:
  → 최근 완료 8 WU (WU-7, 7.1, 10, 10.1, 13, 13.1, 14, 14.1) 만 이관.
  → WU-0 ~ WU-5.1 · WU-8/8.1 · WU-11/11.1/11.2 · WU-12/12.1/12.2/12.3 등 앞선 WU 는 범위 밖.
  → 필요 시 추후 WU (예: WU-16b) 에서 확장 이관.
  → 원칙 2: 범위 해석은 사용자 "이어서 ㄱㄱ" 발화의 암묵 승인 (default_action 에서 (a) WU-16 선택 = §4 out-of-scope 범위 수용).
---

# WU-16 · 기존 WU (WU-7 ~ WU-14.1) 이관

> **목표**: Workflow v1 `WORK-LOG.md` append-style entry 를 v2 `sprints/WU-<id>.md` 독립 파일 구조로 이관. Phase 1 완성 (WU-15) 후 Phase 2 "기존 자산 이관".
> **범위**: 최근 8 WU (§4 scope_interpretation). 본문은 WORK-LOG entry 를 거의 그대로 복사 + frontmatter 만 추가 (Option β, 의미 변형 없음).
> **Out-of-scope**: HANDOFF / BRIEFING 축소 (WU-17), v2 운영 검증 (WU-18), 과거 앞 WU 확장 이관.

---

## 1. 배경 (왜 지금)

- 2026-04-21 WU-15 완료로 v2 인프라 (디렉토리 + 스크립트 + SSoT) 구축 완료. 기존 자산은 아직 v1 형식 (`WORK-LOG.md` append-only single-file).
- Phase 1 킥오프 (2026-04-27) D-3 시점. WU-17 (HANDOFF/BRIEFING 축소) · WU-18 (v2 운영 1주 검증) 을 킥오프 전 또는 병행으로 완주 목표.
- 본 WU 는 "가장 단순 + 가장 반복적" 작업 — v2 규율 검증 + learning-logs/ 패턴 후보 관찰 기회.
- 7번째 세션 (`serene-fervent-wozniak` 종료) 이후 2.5일 공백. 사용자 `이어서 ㄱㄱ` 발화 → `resume_hint.default_action (a) WU-16 착수` 자동 분기.

## 2. Micro-step 실행 로그

| # | step | tag | 결과 |
|:-:|:---|:---|:---|
| 1 | mutex claim (PROGRESS.md `current_wu_owner` self 설정) + `.gitignore` bkit 차단 + 본 stub 작성 | wu16-stub | ✅ |
| 2 | `sprints/WU-7.md` / `WU-7.1.md` 생성 (WORK-LOG L467-L513 기반) | wu7-migrate | ✅ |
| 3 | `sprints/WU-10.md` / `WU-10.1.md` 생성 (WORK-LOG L553-L623 기반) | wu10-migrate | ✅ |
| 4 | `sprints/WU-13.md` / `WU-13.1.md` 생성 (WORK-LOG L424-L466 기반) | wu13-migrate | ✅ |
| 5 | `sprints/WU-14.md` / `WU-14.1.md` 생성 (WORK-LOG L514-L552 기반) | wu14-migrate | ✅ |
| 6 | `sessions/2026-04-20-funny-compassionate-wright.md` (3-part retrospective, 3+4번째 블록 병합) | session-1-retro | ✅ |
| 7 | `sessions/2026-04-21-serene-fervent-wozniak.md` (3-part retrospective, 5번째 블록) | session-2-retro | ✅ |
| 8 | `sessions/2026-04-21-relaxed-vibrant-albattani.md` (3-part retrospective, 6+7번째 병렬) | session-3-retro | ✅ |
| 9 | `sprints/_INDEX.md` 갱신 ("Backfill 대상" → "v1→v2 이관" 테이블 + WU-13/13.1 누락 추가 + v1 형식 WU 목록 보존) | sprints-index-refresh | ✅ |
| 10 | `sessions/_INDEX.md` 갱신 (과거 3 세션 placeholder → 실제 파일 참조 + 1-2번째 세션 행 추가 + 본 8번째 세션 행 추가) | sessions-index-refresh | ✅ |
| 11 | 본 WU-16.md Micro-step 표 + §3 Acceptance + §5 Decision log 완성 + PROGRESS.md 최종 덮어쓰기 | wu16-finalize | ✅ |
| 12 | FUSE bypass 적용 + atomic commit `WU-16: 기존 WU 이관` | wu16-commit | ✅ `2b8b69e` |

## 3. Acceptance (완료 조건)

- [x] `sprints/WU-{7,7.1,10,10.1,13,13.1,14,14.1}.md` 8 파일 모두 존재 + frontmatter 스키마 (§5 CLAUDE.md) 준수 + `migrated_from` / `migrated_by` 필드
- [x] `sessions/2026-04-{20,21,21}-*.md` 3 파일 모두 존재 + 3-part 구조 (§1 squashed WU / §2 대화 요약 / §3 decision log) + `reconstruction_limits` frontmatter
- [x] `sprints/_INDEX.md` 의 완료 테이블 3 섹션 (v2 네이티브 / v1→v2 이관 / v1 형식 보존) + WU-13/13.1 누락 수정 + BLOCKED/Phase 2 명시
- [x] `sessions/_INDEX.md` 3 세션 placeholder → 실제 파일 경로 연결 + 1-2번째 세션 행 (미생성 명시) + 본 8번째 세션 행 추가
- [x] 각 이관 파일 `visibility: raw-internal` 기록 (8 WU + 3 세션 = 11 파일)
- [x] WORK-LOG.md 원본 entry **그대로 보존** (archive 역할, 본 WU 에서 수정 없음)
- [x] PROGRESS.md `① Just-Finished` 에 WU-16 commit sha `2b8b69e` 반영 → WU-16.1 refresh 에서 처리 완료
- [x] WU-16 최종 라인 수 ≤ 200 (§4 4번) — 현 시점 ~150 line 예상

## 4. Out-of-scope (다음 WU 로 이관)

- **WU-16b (필요 시)**: WU-0 ~ WU-5.1 · WU-8/8.1 · WU-11/11.1/11.2 · WU-12/12.1/12.2/12.3 등 앞선 WU 이관. 본 WU 범위 초과.
- **WU-17**: HANDOFF-next-session.md + NEXT-SESSION-BRIEFING.md 축소 (-80% 목표).
- **WU-18**: v2 운영 1주 검증 + learning-logs/ 패턴 3건 실체화.

## 5. Decision log

- **범위 해석 (scope_interpretation, frontmatter 참조)**: `sprints/WU-15.md §4 Out-of-scope` 문구 "WU-7 ~ WU-14.1" 을 **Option β minimal** 로 해석 → 최근 완료 8 WU 만. WU-0~5.1 / 8/8.1 / 11-series / 12-series / HANDOFF 등은 범위 밖 (필요 시 WU-16b 에서 확장). 원칙 2 회피 — 사용자 `이어서 ㄱㄱ` 발화를 default_action (a) WU-16 수용으로 해석, 범위 A/B/C 결정 없음.
- **`sessions/_INDEX.md` 사실 오류 수정**: 기존 행 3개의 부정확한 기재 (WU-7 을 funny-compassionate-wright 에 배치 / CLAUDE.md 신설을 serene-fervent-wozniak 에 배치 등) 는 **사실 확인 기반 수정** 이므로 원칙 2 위반 아님. WORK-LOG Changelog v1.7~v1.19 를 SSoT 로 사용하여 각 WU 의 실제 세션 codename 을 확정.
- **`sprints/_INDEX.md` WU-13/13.1 누락**: WU-15 에서 작성된 인덱스의 "Backfill 대상" 에 WU-13/13.1 이 빠져 있었음 (6 WU 만 명시). 본 WU 에서 8 WU 로 수정 (사실 정정).
- **bkit plugin 메모리 차단 (`.gitignore` 루트)**: `docs/.bkit-memory.json` / `docs/.pdca-status.json` 은 bkit Starter hook 이 세션마다 자동 생성하는 파일. Solon IP 와 무관하므로 전역 pattern (`.bkit-memory.json` / `.pdca-status.json` + `docs/.bkit-*` + `docs/.pdca-*`) 추가. 기존 untracked `docs/` 디렉토리는 **삭제하지 않음** (bkit plugin 이 다시 생성할 수 있어 무의미). 원칙 2 비관련 기술 cleanup.
- **mutex claim 방식**: CLAUDE.md §1 #12 신설 (v1.16) 이후 첫 정식 세션 claim. `current_wu_owner` 필드 5 sub-field (session_codename · claimed_at · last_heartbeat · current_step · ttl_minutes) 전부 기록 + 세션 진입 시 `null` 상태 확인 후 claim. TTL 15분 default 유지.
- **A/B/C 의미 결정 0건**: WU-10 의 W10 TODO 6건 (#14~#19) 결정이나 WU-9 의 Terminal 통합 결정은 본 WU 범위 밖. 이관 작업만 수행.

## 6. Post-commit 체크리스트

1. `git log --oneline -3` 로 WU-16 final sha 확인.
2. frontmatter `final_sha` + `closed_at` + `status: done` 반영 → WU-16.1 refresh 에서.
3. PROGRESS.md `① Just-Finished` + `current_wu: null` (WU-17 으로 넘어갈 때만) 갱신.
4. 필요 시 `scripts/snapshot.sh "event:wu-transition"` 1회 실행 — smoke test.
5. Solon Status Report v0.6.3 출력 (WU-16 완료 dashboard).
6. `git push` 는 **사용자 터미널** 에서만 (§1.5).
