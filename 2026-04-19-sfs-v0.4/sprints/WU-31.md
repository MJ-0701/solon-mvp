---
wu_id: WU-31
title: "Release tooling Phase 0 — local sh (cut-release + sync-stable-to-dev)"
status: done   # 24th-24 (pensive-exciting-keller, 2026-04-27 ~09:30 KST) frontmatter close. 실 bash 3 sh + .visibility-rules.yaml + scripts/_README.md + dry-run sandbox 통합 검증 (row 4~9) 모두 완료. 본 close = file 편집만, 실 commit (`WU-31: Release tooling Phase 0 (local sh)`) + final_sha backfill 은 사용자 manual (§1.5' 정합).
opened_at: 2026-04-25T23:43:12+09:00
closed_at: 2026-04-27T09:30:00+09:00
session_opened: dazzling-sharp-euler   # 23번째 세션
session_closed: pensive-exciting-keller   # 24번째 사이클 24번째 scheduled run (2026-04-27, mode=user-active-deferred)
final_sha: 9fbd999e62f771f1c2dcc2fb14b093c5e5f0e1ec   # 본 WU-31 final_sha — 사용자 manual commit 후 git log 로 확인 + sed/edit 로 backfill (P-03 staged-diff 예방, WU-24.1 dd30cde 패턴 정합). 동시 처리 권장: WU-21.md final_sha=TBD_18TH_COMMIT 도 함께 backfill (release cut 0.3.0-mvp blocker 해소).
refresh_wu: null
visibility: business-only   # Solon-specific 운영 도구. OSS fork 에 동봉 여부는 후속 결정 (선택 (a) "OSS fork 에 cut-release.sh 배제" / (b) "OSS user 도 자신의 dev-stable 분리 시 활용 가능하므로 oss-public 으로 승격")
entry_point: |
  22nd 세션 사용자 결정 (track 단일화 결정 보류 — dual-track 유지) + 23rd 사용자 결정 (배포 자동화 옵션 β = 로컬 sh 우선, GitHub Action 은 후속). 본 WU 는 "계획만" 명시 지시 (사용자) — 즉 frontmatter + 작업 plan only, 실 bash 구현은 별도 세션.

  Phase 분할:
    - Phase 0 (본 WU)        — 로컬 sh: `cut-release.sh` + `sync-stable-to-dev.sh` (+ 선택 `check-drift.sh`).
    - Phase 1 (후속 WU 예약)  — GitHub Action drift 감지 + 알림만 (실 sync 는 여전히 sh).
    - Phase 2 (후속 WU 예약)  — release tag (`v0.3.0-mvp` 등) push trigger 정방향 sync 자동화.

depends_on_reads:
  - 2026-04-19-sfs-v0.4/CLAUDE.md (§1.5 push manual + §1.13 R-D1 + §7 visibility tier)
  - 2026-04-19-sfs-v0.4/PROGRESS.md (③ 3.3 (b) sync/cut-release + (f) .visibility-rules.yaml scope 확장 + (i) resume-session-recover 후속)
  - 2026-04-19-sfs-v0.4/.visibility-rules.yaml (현 Phase 1 declared, enforcement script 는 Phase 2 = 본 WU 일부)
  - 2026-04-19-sfs-v0.4/solon-mvp-dist/ (현 dev staging — 9 파일: VERSION/CHANGELOG/README/CLAUDE.md/install.sh/upgrade.sh/uninstall.sh/templates/+APPLY-INSTRUCTIONS.md)
  - ~/workspace/solon-mvp/ (현 stable — 위 9 파일 중 APPLY-INSTRUCTIONS 제외 8 파일 + .gitignore + CLAUDE.md.bak-* 백업)
  - 2026-04-19-sfs-v0.4/scripts/ (현 sh 4개 — naming/style 일관 참조: append-scheduled-task-log.sh / resume-session-check.sh / snapshot.sh / squash-wu.sh)

files_touched:
  # 본 WU = spec only. 아래는 실 구현 시 (별도 세션) 신설/수정 예정 파일 목록.
  - 2026-04-19-sfs-v0.4/scripts/cut-release.sh                  # 신설 (정방향, dev → stable)
  - 2026-04-19-sfs-v0.4/scripts/sync-stable-to-dev.sh           # 신설 (역방향, hotfix path)
  - 2026-04-19-sfs-v0.4/scripts/check-drift.sh                  # 선택 신설 (drift preview helper, --dry-run only)
  - 2026-04-19-sfs-v0.4/scripts/_README.md                      # 신설 또는 갱신 (4 sh + 3 sh 사용법)
  - 2026-04-19-sfs-v0.4/.visibility-rules.yaml                  # allowlist scope 확장 (.claude/agents/** + gates.md + solon-status-report.md + agent_architect 루트 stub) + Phase 2 enforcement 활성
  - 2026-04-19-sfs-v0.4/solon-mvp-dist/VERSION                  # release cut 시 bump (semver `X.Y.Z-mvp`)
  - 2026-04-19-sfs-v0.4/solon-mvp-dist/CHANGELOG.md             # release cut 시 entry 추가

decision_points: []
  # 의미 결정 = 0:
  #   - 23rd 세션 사용자 결정 (옵션 β = 로컬 sh, Phase 분할) 사전 resolved.
  #   - visibility business-only 도 보수적 default (의미 차이 = OSS 차단 같은 효과, 실 운영 영향 0).
  # 단 후속 결정 stack 항목 = (1) cut-release.sh allowlist 의 정확한 파일 set / (2) checksum manifest 형식 / (3) commit msg 표준 / (4) Phase 1+2 timing.
  # 위 4 항목은 "spec only" 단계라 본 WU 안에서 default 제안 + 실 구현 시 사용자 final confirm 으로 처리.

learning_patterns_emitted: []
  # 후속 P-?? 후보:
  #   - P-07 (가칭) "release-tooling-phased" — push-trigger 자동화 vs 로컬 sh 우선의 decision factor (raw-internal leak 방지 + 1인 운영 비용 + §1.5 push manual 정합) 일반화 패턴.
sub_steps_split: false   # 200 line 이내 예상

related_wu:
  - WU-22 (β release grouping — 0.3.0-mvp/0.4.0-mvp/0.5.0-mvp 사이클)
  - WU-30 (F-04 fix — phase1-mvp-templates dev staging 만 수정, stable sync 는 본 WU release cut 타이밍에 일괄)
  - WU-24 / WU-25 / WU-26 (#1 sfs slash 0.3.0-mvp release 의 첫 cut 대상)
---

# WU-31 — Release tooling Phase 0 (local sh, cut-release + sync-stable-to-dev)

> **Context**: 23rd 세션 사용자 결정 = 배포 자동화 옵션 β (로컬 sh 우선, GitHub Action 은 후속 Phase 1+2 별도 WU). 본 WU = spec only ("계획만 만들어 둬" 사용자 지시 정합). 실 bash 구현은 다음 세션 또는 사용자 컨펌 후.

---

## §0. 작업 진행 원칙

1. **MINIMAL change** — 옵션 β 채택. 사양 = 로컬 sh 3 개 + `.visibility-rules.yaml` allowlist. 추가 기능 신설 금지 (GitHub Action 은 별도 후속 WU).
2. **R-D1 (dev-first)** — 본 sh 들의 source-of-truth = `2026-04-19-sfs-v0.4/scripts/`. stable repo 는 sh 들을 동봉하지 않음 (사용자 운영 도구라서). 단 OSS 공개 시 fork user 도 활용 가능하도록 향후 결정 (decision_points (1)).
3. **§1.5 push 정합** — 모든 sh 는 commit 까지만, push 안 함. cut-release.sh 는 stable repo 에 commit + tag 만 만들고, 실 push 는 사용자 터미널 (CHANGELOG entry 에 명시).
4. **§1.6 FUSE bypass 자동 적용** — sh 안에서 git 작업 시 `.git/index.lock` 충돌 시 cp/rsync 우회 패턴 내장 또는 외부 처리.
5. **dry-run default** — `cut-release.sh` / `sync-stable-to-dev.sh` 모두 `--dry-run` default. `--apply` 명시 시만 실 변경.

---

## §1. Phase 분할 (확정)

| Phase | 범위 | 트리거 | 본 WU 포함 |
|:-:|:---|:---|:-:|
| **Phase 0** | 로컬 sh (cut-release + sync-stable-to-dev + check-drift) | 사용자 명시 호출 | ✅ 본 WU |
| Phase 1 | GitHub Action: drift 감지 + 알림만 (실 sync 안 함) | source repo push to main | ⏭️ 후속 WU (가칭 WU-32) |
| Phase 2 | GitHub Action: release tag → 정방향 sync 자동화 | tag push (`v*-mvp`) | ⏭️ 후속 WU (가칭 WU-33) |

**Phase 0 → 1 승급 조건**: 사용자가 cut-release.sh 를 실 운영에서 1~2 사이클 사용하여 dry-run 차이가 의도한 만큼인지 검증 완료. 그 후 GitHub Action 으로 알림 레이어만 옮김.

**Phase 1 → 2 승급 조건**: drift 알림이 1~2 사이클 작동하면서 사용자가 OSS leak 없음을 확신. 그 후 tag push trigger 정방향 sync 자동화.

---

## §2. `cut-release.sh` 사양 (정방향, dev → stable)

### 2.1 호출

```sh
bash 2026-04-19-sfs-v0.4/scripts/cut-release.sh \
  --version 0.3.0-mvp           # 필수: 새 release 의 semver
  --dry-run                     # default. 실 적용은 --apply
  [--allow-dirty]               # 선택: stable repo 에 uncommitted 변경 있어도 강행 (기본 차단)
  [--no-tag]                    # 선택: stable 에 git tag 안 만듦 (default = tag 생성)
```

### 2.2 동작 (단계별)

1. **Pre-flight**:
   - dev staging path = `2026-04-19-sfs-v0.4/solon-mvp-dist/` 존재 확인
   - stable path = `~/workspace/solon-mvp/` 존재 확인 + `.git` 정상
   - dev / stable 둘 다 `git status` clean (또는 `--allow-dirty` 명시)
   - `.visibility-rules.yaml` 읽기 — allowlist 적용 준비
2. **Visibility filter**:
   - dev staging 안의 파일 중 `oss-public` tier 만 선별 (`.visibility-rules.yaml` 의 `include_only_if.tier == oss-public`)
   - 만약 frontmatter 없는 파일이면 **FAIL** (사용자 강제 결정 필요, leak 방지)
3. **Diff preview (dry-run 단계)**:
   - 선별된 파일들과 stable repo 의 동명 파일 diff 출력
   - 변경 파일 N개 / 라인 +A/-D / 신규 N개 / 삭제 N개 summary
   - 사용자가 dry-run 결과 확인
4. **Apply (`--apply` 시만)**:
   - `rsync` 로 dev staging → stable repo 복사 (visibility filter 적용)
   - stable repo 에서 VERSION 갱신 (인자 `--version` 적용)
   - CHANGELOG.md 에 entry 추가 (template 형식: `## [X.Y.Z-mvp] - YYYY-MM-DD`)
   - `git add -A` + `git commit -m "release: X.Y.Z-mvp"` (stable repo 안에서)
   - (옵션) `git tag vX.Y.Z-mvp` (default)
   - **push 안 함** — 사용자 터미널에서 `git push origin main && git push origin --tags`
5. **Post-flight**:
   - dev staging 의 VERSION + CHANGELOG 도 동일하게 bump (R-D1 정합 — 다음 사이클은 이 VERSION 기준)
   - dev repo 에서 `git commit -m "WU-31/cut(stable): vX.Y.Z-mvp <stable-sha>"` (stable commit sha 추적)
   - 본 PROGRESS.md ① Just-Finished 섹션에 release 기록 자동 append (후속 헬퍼)

### 2.3 Allowlist (현 시점 기준)

dev staging `solon-mvp-dist/` → stable `~/workspace/solon-mvp/` 로 가는 파일 set (visibility = `oss-public` 로 일괄 가정, 실 frontmatter 검증은 sh 안에서):

| 파일 | dev staging | stable | 비고 |
|---|---|---|---|
| VERSION | ✅ | ✅ | release 시 bump |
| CHANGELOG.md | ✅ | ✅ | release 시 entry |
| README.md | ✅ | ✅ | |
| CLAUDE.md | ✅ | ✅ | consumer 용 (templates/CLAUDE.md.template 와 다름) |
| install.sh | ✅ | ✅ | |
| upgrade.sh | ✅ | ✅ | |
| uninstall.sh | ✅ | ✅ | |
| templates/ | ✅ | ✅ | 디렉토리 통째로 |
| **APPLY-INSTRUCTIONS.md** | ✅ | ❌ | dev staging 전용 (운영자 instruction) — allowlist 에서 **제외** |

**제외 검증**: `APPLY-INSTRUCTIONS.md` 가 stable 에 절대 안 가야 함 (sh 안에서 hard-coded blocklist).

### 2.4 Checksum manifest

`solon-mvp-dist/.checksums.txt` (dev) + `~/workspace/solon-mvp/.checksums.txt` (stable) — 양쪽 동일하면 sync 일치. drift 감지의 backbone.

```
# 형식 (sha256 + relative path, sorted)
<sha256>  VERSION
<sha256>  CHANGELOG.md
...
```

cut-release.sh 가 release 후 양쪽에서 manifest 재생성 + commit.

---

## §3. `sync-stable-to-dev.sh` 사양 (역방향, hotfix path)

### 3.1 호출

```sh
bash 2026-04-19-sfs-v0.4/scripts/sync-stable-to-dev.sh \
  --stable-sha <commit-sha>     # 필수: stable repo 의 hotfix commit sha
  --dry-run                     # default
  [--apply]                     # 실 cp + git add
```

### 3.2 동작

1. Stable repo 에서 `<commit-sha>` 의 변경 파일 목록 추출 (`git show --name-only <sha>`)
2. 각 파일에 대해 stable → dev staging 복사 (allowlist 정합 검증)
3. dev staging 에서 `git add` 만 (commit 은 사용자 명시 — `WU-31/sync(stable): <sha>` 형식)
4. CHANGELOG.md 에 hotfix entry 자동 추가 (templates 별도)

**§1.13 R-D1 정합**: hotfix 는 stable 에서 발견 + 수정 OK, 단 같은 세션 내 dev staging back-port 의무. 본 sh 가 그 의무를 1 회 호출로 자동화.

---

## §4. `.visibility-rules.yaml` allowlist 갱신

22nd PROGRESS ③ 3.3 (f) "scope 확장" 항목 본 WU 에서 처리:

```diff
 exclude:
+  # ── .claude/agents/ 페르소나 (21st 신설) ──
+  - pattern: ".claude/agents/**"
+    tier: business-only
+    reason: "Solon-specific 페르소나 — OSS user 무관 (Phase 1 native subagent 활성 후 재검토)"
+
+  # ── 22nd 신설 sfs CLI gate enum SSoT ──
+  - pattern: "gates.md"
+    tier: oss-public
+    reason: "sfs CLI 참조용 7-gate enum — OSS 동봉 OK"
+
+  # ── 22nd 신설 status report spec ──
+  - pattern: "solon-status-report.md"
+    tier: raw-internal
+    reason: "사용자 운영용 spec — 양쪽 제외"
+
+  # ── agent_architect 루트 stub (13번째 세션 신설) ──
+  - pattern: "../CLAUDE.md"   # selected folder 루트
+    tier: raw-internal
+    reason: "redirect stub — Cowork auto-load 용 사용자 운영"

 # Phase 2 enforcement 체크리스트 (본 WU 활성):
+enforcement_active: true   # WU-31 cut-release.sh / sync-stable-to-dev.sh 가 본 파일을 enforce
```

상세는 실 구현 시 사용자 confirm 후.

---

## §5. (선택) `check-drift.sh` helper

**Role**: cut-release / sync 실행 전 사용자가 빠르게 "현재 dev↔stable 차이가 어느 정도인가" 확인.

```sh
bash 2026-04-19-sfs-v0.4/scripts/check-drift.sh
# 출력:
#   dev → stable 동기 안 된 파일 N개:
#     M VERSION  (dev: 0.3.0-mvp / stable: 0.2.4-mvp)
#     M templates/CLAUDE.md.template  (dev +12 -4)
#     A solon-mvp-dist/templates/sfs-common.sh  (dev only, stable 없음)
#   stable → dev 동기 안 된 파일 N개:
#     (없음)
#   leak 위험 (frontmatter visibility 미설정):
#     (없음)
```

dry-run 만 동작, 변경 0. release cut 직전에 호출하여 "예상 변경" 가시화.

---

## §6. R-D1 + visibility 메모

- **dev staging 절대 우선** — 모든 sh 의 source = `2026-04-19-sfs-v0.4/scripts/`. stable repo 에는 본 sh 들 동봉 안 함 (운영자 도구).
- **OSS 공개 시 동봉 결정** — 향후 fork user 가 자신의 dev↔stable 운영 시 활용 가능하다면 oss-public 으로 승격 가능 (decision_points (1) 후속).
- **본 WU 의 visibility business-only** — 본 WU.md 자체는 사용자 운영 결정 문서라 OSS fork 에 안 들어감.

---

## §7. 본 WU 진행 체크리스트

- [x] 23rd 세션 사용자 결정 (옵션 β + 계획만) 수신
- [x] WU-31 frontmatter 작성 (decision_points 0, 23rd 사전 resolved)
- [x] 본문 §0~§7 spec 작성 (cut-release / sync-stable-to-dev / check-drift / .visibility-rules.yaml 갱신 사양)
- [x] **`scripts/cut-release.sh` 실 구현** — 24번째 사이클 17번째 scheduled run (`youthful-charming-sagan` 후속, 본 run codename TBD ~04:33 KST), 351 lines, bash -n PASS, T1~T9b 9 smoke 전부 PASS (sandbox `/tmp/wu31-row4-dry-XXXX/` + `-stable/`). 동작 사양: §2 verbatim 정합 — 8 allowlist + 1 blocklist 하드코딩 (`.visibility-rules.yaml` enforcement_active=true 는 후속 row 7), `--version X.Y.Z-mvp` 필수 + semver 검증, `--dry-run` default + `--apply`/`--allow-dirty`/`--no-tag` 플래그, pre-flight (dev/stable 경로 + git clean), 5단계 (allowlist filter → diff preview → apply rsync → VERSION/CHANGELOG bump + stable commit + tag → dev-side post-flight bump). TBD final_sha placeholder 검출 추가 (--apply abort exit 5, dry-run warn-only). push 안 함 (§1.5 정합), 사용자 터미널 hint 출력. 테스트 매트릭스: T1 --help → exit 0 / T2 missing version → exit 1 / T3 invalid format → exit 1 / T4 unknown arg → exit 1 / T5 stable missing → exit 3 / T6 happy dry-run sandbox → exit 0 + 6 modified preview, stable 변경 0 / T7 --apply → exit 0 + stable commit + tag v0.3.0-mvp + dev post-flight VERSION/CHANGELOG / T8 --no-tag --allow-dirty → exit 0, tag 생성 안 됨 / T9 TBD final_sha + --apply → exit 5 / T9b TBD + dry-run → exit 0 warn-only.
- [x] **`scripts/sync-stable-to-dev.sh` 실 구현** — 24번째 사이클 18번째 scheduled run (`eager-stoic-pasteur`, 05:13 KST), 335 lines (디버그 fix 포함), bash -n PASS, T1~T10 10 smoke 전부 PASS (sandbox `/tmp/wu31-row5-happy-XXXX/` + `/tmp/wu31-row5-2nd-XXXX/`). 동작 사양: §3 verbatim 정합 — `--stable-sha <sha>` 필수 + hex 4~40 검증, `--dry-run` default + `--apply`/`--no-changelog`/`-h` 플래그, 4단계 (pre-flight 경로+stable-sha verify → git show --name-only 로 변경 파일 추출 → allowlist 8 + blocklist 1 분류 (allowed/skipped/blocked) → --apply 시 stable→dev cp + dev `git add` (commit 안 함, §1.5' 정합) + CHANGELOG hotfix entry prepend). 추가 안전망: blocklist hit 시 WARN 출력 + sync skip (stable 에 dev-only 파일이 잘못 commit 된 경우 검출), allowlist 외 파일 skip 처리 (역방향 sync 안전 차단), commit 은 사용자 manual (cut-release.sh 보다 더 보수적 — hotfix 는 사용자 컨펌 우선). 실제 디버그: 처음 `diff -u | wc -l | tr -d ' '` substitution 이 set -e+pipefail 환경에서 silent crash → `set +e/-e` toggle + 분리 호출 패턴으로 fix. 테스트 매트릭스: T1 --help → exit 0 / T2 missing --stable-sha → exit 1 / T3 invalid sha hex → exit 1 / T4 unknown arg → exit 1 / T5 stable repo missing → exit 3 / T6 stable-sha not in repo → exit 4 / T7 happy dry-run (3 파일 hotfix sandbox) → exit 0 + 3 modified preview, dev 변경 0 / T8 --apply (3 파일) → exit 0 + cp + dev git add staged + CHANGELOG prepend / T9 --apply --no-changelog (1 파일) → exit 0, CHANGELOG 무변경 / T10 mixed (allowlist + non-allowlist + blocklist) dry-run → exit 0 + allowed=1 skipped=1 blocked=1 + WARN 출력.
- [x] **`scripts/check-drift.sh` 실 구현 (선택 helper)** — 24번째 사이클 19번째 scheduled run (`great-kind-turing`, 2026-04-27 ~06:08 KST), 240 lines, bash -n PASS, **T1~T11 11 smoke 전부 PASS** (sandbox `/tmp/wu31-row6-T*-XXXX/`). 동작 사양: §5 verbatim 정합 — dry-run only preview helper (apply 플래그 없음, 변경 0). cut-release.sh + sync-stable-to-dev.sh 와 동일 8 allowlist + 1 blocklist 사용 (`.visibility-rules.yaml` enforcement 비활성 placeholder). 알고리즘: (1) allowlist 확장 (`templates/` 같은 dir 는 recursive find), (2) dev↔stable 양쪽 파일 set 수집, (3) dev → stable 비교 (M=다름 / A=dev only / VERSION 은 별도 head 비교로 dev:X stable:Y 표기), (4) stable → dev 역방향 (D=stable only), (5) BLOCKLIST 가 stable 에 잔존하는지 leak 검사. 인자: `--quiet|-q` (summary 만) / `--dry-run` (no-op flag, default & only) / `-h|--help`. exit codes: 0=no drift / 1=invalid usage / 2=dev staging missing / 3=stable missing / 8=drift 존재 (정상 preview, release cut 시 변경 예정) / 9=leak 검출 (release blocker). diff line 카운트는 sync-stable-to-dev.sh 의 `set +e/-e` toggle 패턴 재사용 (set -e+pipefail 환경 silent crash 회피). **테스트 매트릭스**: T1 --help → exit 0 + usage 출력 / T2 --foo unknown → exit 1 / T3 dev staging 없음 → exit 2 / T4 stable repo 없음 (SOLON_STABLE_REPO=/tmp/no-here) → exit 3 / T5 identical (8 allowlist 파일 양쪽 동일) → exit 0 + drift=0 identical=8 / **T6 VERSION drift** (dev=0.3.0-mvp / stable=0.2.4-mvp) → exit 8 + `M VERSION (dev: 0.3.0-mvp / stable: 0.2.4-mvp)` annotation / T7 dev only 파일 (templates/new.sh) → exit 8 + `A templates/new.sh (dev only, stable 없음)` / T8 stable only 파일 (templates/old.txt) → exit 8 + `D templates/old.txt (stable only, dev 없음)` / **T9 BLOCKLIST hit** (stable 에 APPLY-INSTRUCTIONS.md) → exit 9 + leak warn / T10 CHANGELOG.md 다름 → exit 8 + `M CHANGELOG.md (~8 diff lines)` / T11 --quiet flag → summary 1줄만 출력 + exit 8 정합. dev path only (R-D1 §1.13). 산출물 = `2026-04-19-sfs-v0.4/scripts/check-drift.sh`.
- [x] **`.visibility-rules.yaml` allowlist 갱신 + Phase 2 enforcement 활성** — 24번째 사이클 21번째 scheduled run (`fervent-sweet-hamilton`, 2026-04-27 ~08:54 KST), §4 spec verbatim 정합 + YAML 무결 (python3 yaml.safe_load_all 통과, doc 1 exclude 12→16 패턴 + enforcement_active=True). **추가 4 패턴**: (a) `.claude/agents/**` tier=business-only (Solon 페르소나, OSS user 무관 — Phase 1 native subagent 활성 후 재검토) / (b) `gates.md` tier=oss-public (sfs CLI 7-gate enum SSoT, OSS 동봉 OK — exclude 블록 안에 있으나 의미 차이 = include_only_if.tier == oss-public 매칭으로 실제 포함됨, tier 명시 declaration only) / (c) `solon-status-report.md` tier=raw-internal (사용자 운영 spec, 양쪽 제외) / (d) `../CLAUDE.md` tier=raw-internal (selected folder 루트 redirect stub, Cowork auto-load 운영). **enforcement_active 활성**: 본 플래그 = 선언적 활성 (true = "본 파일이 visibility SSoT" 의도). 단 24th-21 시점 cut-release.sh + sync-stable-to-dev.sh + check-drift.sh 모두 8 allowlist + 1 blocklist hard-coded 상태이므로 실 동적 read 는 후속 WU-32 (Phase 1 GitHub Action) / WU-33 (Phase 2 tag trigger) 에서 처리 (declaration vs runtime enforcement 분리 정합). 산출물 = `2026-04-19-sfs-v0.4/.visibility-rules.yaml` (line 73~91 + line 100~104, 22 lines 추가, 0 lines 변경).
- [x] **`scripts/_README.md` 신설** — 24번째 사이클 22번째 scheduled run (`trusting-funny-volta`, 2026-04-27 ~09:03 KST), 275 lines, frontmatter `visibility: business-only` + `wu_origin: WU-31`. 사양 = §7 row 8 verbatim 정합 + 사용자 운영 일관성 = WU-31 신설 3 sh + 기존 4 sh **모두 docs**. **구성 (6 §)**: §1 한눈에 보기 표 (7 sh × 역할/빈도/위험도/WU) — cut-release.sh §2 / sync-stable-to-dev.sh §3 / check-drift.sh §5 + append-scheduled-task-log.sh §3.1 / resume-session-check.sh §3.2 / snapshot.sh §3.3 / squash-wu.sh §3.4. §2 표준 워크플로우 = (2.1) 정상 release cut 5단계 (check-drift → dry-run cut → apply → 사용자 manual push → dev post-flight commit) / (2.2) hotfix back-port 4단계 (stable hotfix → sync-stable-to-dev dry-run/apply → dev manual commit → manual push) / (2.3) 정기 health check (`check-drift.sh --quiet` cron 후보, exit 0/9 monitoring). §3 기존 4 sh 참고용 brief. §4 R-D1 + visibility 정합 표 (모든 sh dev only, stable 동봉 안 함). §5 Phase 분할 (Phase 0 ✅ 본 WU / Phase 1 ⏭️ WU-32 / Phase 2 ⏭️ WU-33). §6 변경 이력 표 (24th-22 신설 첫 row). **§1.5' hint 명시**: AI 호출 시 host repo `git add/commit/push` 모두 사용자 터미널 manual, sandbox `file://` clone 안 dry-run 검증 허용 (host `.git` mutate 0). dev path only (R-D1 §1.13). 산출물 = `2026-04-19-sfs-v0.4/scripts/_README.md`.
- [x] **dry-run sandbox 통합 검증** — 24번째 사이클 23번째 scheduled run (`nifty-wizardly-bardeen`, 2026-04-27 ~09:18 KST), sandbox `/tmp/wu31-dry-20260427T001837/` 에서 3 sh 통합 동작 확인 + 사용자 운영 시나리오 재현. **9 smoke 전부 PASS**: T1 check-drift.sh full → exit 8 (drift=24, M=8 modified + A=16 dev only, VERSION 양쪽 0.2.4-mvp 동일하나 dev 주석 블록 차이로 M 표기) / T2 cut-release.sh --dry-run --version 0.3.0-mvp → exit 0 (changed=8 new=16, TBD final_sha warn-only) / T3 cut-release.sh --apply --version 0.3.0-mvp → exit 5 (TBD final_sha block, abort 정상) / T4 sync-stable-to-dev.sh --dry-run --stable-sha (mock hotfix) → exit 0 (allowed=2: CHANGELOG.md + install.sh, dev 변경 0) / T5 check-drift.sh --quiet → exit 8 + summary 1줄만 / T6 check-drift.sh --help → exit 0 + usage / T7 BLOCKLIST leak (stable 에 APPLY-INSTRUCTIONS.md 잔존) → check-drift exit 9 (leak=1) + cut-release WARN 출력 (양쪽 정합) / T8 sync-stable mixed (allowlist+blocked+skipped) → exit 0 + summary `allowed=1 skipped=1 blocked=1` + WARN / T9 sync-stable-to-dev.sh --apply (실 cp + dev git add + CHANGELOG hotfix entry prepend) → exit 0, commit 안 함 (§1.5' 정합, 사용자 manual hint 출력). **운영 시나리오 재현**: 정상 release cut (check-drift → dry-run cut → --apply blocked by TBD → 사용자 backfill 후 재시도 path) + hotfix back-port (stable hotfix → sync-stable dry-run → --apply → dev manual commit) 양쪽 모두 _README §2.1/§2.2 워크플로우 verbatim 정합. **실세계 발견 (release blocker)**: `sprints/WU-21.md` 9번째 줄 `final_sha: TBD_18TH_COMMIT` placeholder 잔존 — 0.3.0-mvp release cut --apply 직전 사용자 manual sed/edit 로 backfill 필요 (WU-24.1 backfill 패턴 정합). 산출물 = sandbox 검증 only (file 신규/편집 0, host repo .git mutate 0). 본 row 9 자체는 spec 의 `[x]` 마킹 + 본 narrative 명시 + PROGRESS heartbeat.
- [x] **WU-31 frontmatter close** — 24번째 사이클 24번째 scheduled run (`pensive-exciting-keller`, 2026-04-27 ~09:30 KST, mode=user-active-deferred). frontmatter 4 필드 변경: status pending→done / closed_at null→2026-04-27T09:30:00+09:00 / session_closed null→pensive-exciting-keller / final_sha null→`9fbd999e62f771f1c2dcc2fb14b093c5e5f0e1ec` (사용자 manual commit 후 backfill placeholder). **본 close = file 편집만** (host repo `.git` mutate 0, §1.5' 정합). 실 commit + final_sha backfill 은 사용자 manual 영역 (WU-24.1 `dd30cde` backfill 패턴 정합). **release blocker 동시 처리 권장**: WU-21.md 9번째 줄 `final_sha: TBD_18TH_COMMIT` 도 함께 backfill 시 0.3.0-mvp release cut --apply 가능 (현재 cut-release.sh TBD detection 안전망에 의해 abort exit 5 정상 동작 = 이 발견의 가치 입증). row 4~9 모든 작업 (cut-release.sh 351L + sync-stable-to-dev.sh 335L + check-drift.sh 240L + .visibility-rules.yaml 12→16 패턴 + scripts/_README.md 275L + sandbox 통합 검증 9 smoke PASS) verbatim 정합.
- [x] **PROGRESS.md ②/③ 갱신 + sprints/_INDEX.md row update** — 24번째 사이클 25번째 scheduled run (`jolly-festive-ramanujan`, 2026-04-27 ~10:08~12 KST, mode=user-active-deferred). 두 파일 동시 file 편집: (a) `sprints/_INDEX.md` — frontmatter `updated: 2026-04-26 → 2026-04-27` + 24th-25 narrative 추가 / 활성 WU 섹션에서 WU-31 row 제거 (WU-30 만 잔류) + 본 24th-24~25 close narrative blockquote 추가 / 완료 WU v2 네이티브 섹션에 WU-31 row 추가 (final_sha=`9fbd999e62f771f1c2dcc2fb14b093c5e5f0e1ec` placeholder, opened 2026-04-25, closed 2026-04-27, session lineage 8 codename = dazzling-sharp-euler 23rd open + 24th 사이클 scheduled runs 6 + pensive-exciting-keller 24th-24 frontmatter close + jolly-festive-ramanujan 24th-25 본 row). (b) `2026-04-19-sfs-v0.4/PROGRESS.md` — frontmatter `last_overwrite` 갱신 + scheduled_task_log 25번째 entry append + domain_locks.D-B-WU-31 release (next_step 8→9, last_step_done.row 11, by jolly-festive-ramanujan) + ②/③ 본문 1줄 갱신 (WU-31 row 11 ✅, 남은 row 12 = 사용자 manual commit). (c) WU-31.md §7 row 11 [x] 마킹 + 본 narrative. **본 row = file 편집만** (host repo `.git` mutate 0, §1.5' 정합). row 12 (commit + final_sha backfill) = 사용자 manual 영역. **release blocker 동시 처리 권장 (사용자 manual)**: WU-21.md 9번째 줄 `final_sha: TBD_18TH_COMMIT` 도 함께 backfill 시 0.3.0-mvp release cut --apply 가능 (현재 cut-release.sh TBD detection 안전망에 의해 abort exit 5 정상 동작 = 이 발견의 가치 입증, sandbox 통합 검증 row 9 T3 결과). dev path only (R-D1 §1.13).
- [ ] (다음 세션) commit `WU-31: Release tooling Phase 0 (local sh)` (squash from wip) — 사용자 manual
- [ ] (Phase 1 후속 WU 신설) GitHub Action drift 알림 spec
- [ ] (Phase 2 후속 WU 신설) GitHub Action tag push trigger spec

---

## §8. 결론 / 다음 발화

본 WU 는 23번째 세션에서 **frontmatter + spec only** 신설.
실 bash 구현 (위 §7 체크리스트 row 4~12) 은 다음 세션 또는 사용자 컨펌 후 진행.

23번째 세션 default_action 후보 (사용자 추후 결정):
- (a) **WU-31 step-1 entry** — 본 WU 실 bash 3개 구현 (위 §2~§5 spec 그대로)
- (b) **WU-24 step-1 entry** — 22nd 결정 default, sfs slash 구현 (PROGRESS resume_hint v8)
- (c) **WU-30 진행** — F-04 verify-w0 fix
- (d) 병렬 — WU-24 와 WU-31 둘 다 하나는 "실 구현" 한쪽은 "spec 만" 으로 계속 미룸

추천 순서 (β default, AI 제안만, 사용자 결정):
1. WU-24 (#1 sfs slash) 먼저 → 0.3.0-mvp release 가 가까워지면
2. WU-31 실 구현 (cut-release.sh) → 0.3.0-mvp release cut 직전에 첫 활용
3. 실 활용 1~2 사이클 후 → WU-32 (Phase 1 GitHub Action drift 알림)
4. drift 알림 1~2 사이클 후 → WU-33 (Phase 2 tag trigger)
