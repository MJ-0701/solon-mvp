---
wu_id: WU-15
title: "Workflow v2 인프라 설정 (sprints/ sessions/ learning-logs/ snapshots/ + scripts + SSoT 완성)"
status: done              # pending | in_progress | done | abandoned
opened_at: 2026-04-21T08:00:00+09:00
closed_at: 2026-04-21T11:20:00+09:00
session_opened: relaxed-vibrant-albattani
session_closed: relaxed-vibrant-albattani
final_sha: aa0a354        # single atomic commit (wip 분할 없음)
refresh_wu: WU-15.1       # forward sha backfill 전용
visibility: raw-internal   # 운영 infra 규율 — OSS fork 비포함
entry_point: |
  PROGRESS.md → current_wu_path: sprints/WU-15.md.
  §14.3 수용 확정 (CLAUDE.md v1.15) 후 인프라 설정 in_progress.
  남은 작업: 단일 squash 커밋 (CLAUDE + PROGRESS + 신규 infra) + 그 후 WU-16 이관 준비.
depends_on_reads:
  - CLAUDE.md              # §1 절대 규칙 + §3~§14 v2 규율 SSoT
  - PROGRESS.md            # live snapshot + resume_hint
  - tmp/workflow-v2-design.md  # draft-0.3 (v2 9축 + dual-track + snapshot spec)
files_touched:
  # 신규 ──────────────────────────────
  - sprints/_INDEX.md                       # 신규 — WU 인덱스
  - sprints/WU-15.md                        # 본 파일
  - sessions/_INDEX.md                      # 신규 — 세션 3-part 로그 인덱스
  - learning-logs/_INDEX.md                 # 신규 — 학습 패턴 인덱스
  - learning-logs/_TEMPLATE.md              # 신규 — 패턴 파일 템플릿
  - .visibility-rules.yaml                  # 신규 — OSS fork 제외 규칙
  - scripts/snapshot.sh                     # 신규 — file-level auto-snapshot
  - scripts/squash-wu.sh                    # 신규 — WU 단위 interactive rebase 헬퍼
  - tmp/snapshots/.gitkeep                  # 신규 — 디렉토리 유지
  # 수정 ──────────────────────────────
  - CLAUDE.md                               # v1.14 → v1.15 (§14.3 삭제 + §14 헤더에 role 명시)
  - PROGRESS.md                             # frontmatter 에 current_wu / current_wu_path 추가 + last_overwrite 갱신
  - .gitignore                              # sprints/_scratch/ 추가 + WU-15 블록 주석
  - NEXT-SESSION-BRIEFING.md                # 이전 세션 잔여 dangling change (+2 line, W10 사전 분석 참조) 흡수
decision_points: []         # 이번 WU 는 사용자 결정 없음 (§14.3 는 WU 시작 전 이미 수용됨)
learning_patterns_emitted: []   # 패턴 실체화는 WU-18 에서 수행 (후보 3건은 learning-logs/_INDEX.md 에 명세)
sub_steps_split: false      # 200 line 이내 예상
---

# WU-15 · Workflow v2 인프라 설정

> **목표**: Workflow v1 → v2 전환의 **Phase 1 (인프라)** 완성. 실제 기존 WU 이관은 WU-16, HANDOFF/BRIEFING 축소는 WU-17, 운영 검증은 WU-18 로 연속 진행.
> **범위 경계**: 본 WU 는 **디렉토리 + 스크립트 + SSoT 정비** 까지. 기존 WU 파일 이관이나 HANDOFF 축소는 **명시적으로 out-of-scope**.

---

## 1. 배경 (왜 지금)

- Phase 1 킥오프 D-6 (2026-04-27). v2 운영 검증 (WU-18) 포함하려면 인프라는 본 세션 내 완료 필수.
- 직전 세션 (`serene-fervent-wozniak`) 이 v2 설계 9축을 확정 (`tmp/workflow-v2-design.md` draft-0.3) + CLAUDE.md v1.14 (SSoT) 신설 + PROGRESS.md `resume_hint` 필드 신설.
- 본 세션 진입 시 `ㄱㄱ` 자동 트리거 → §14.3 수용 확정 (CLAUDE.md v1.15) → WU-15 착수.

## 2. Micro-step 실행 로그

| # | step | tag | 결과 |
|:-:|:---|:---|:---|
| 1 | CLAUDE.md §14.3 삭제 + §14 헤더 `role: WU dashboard` 명시 + v1.14→v1.15 | claude-md-finalize | ✅ |
| 2 | `sprints/` + `sessions/` + `learning-logs/` + `tmp/snapshots/` + `scripts/` 디렉토리 생성 | mkdir-v2 | ✅ |
| 3 | `sprints/_INDEX.md` 작성 (활성 WU + backfill 대상 placeholder) | sprints-index | ✅ |
| 4 | `sessions/_INDEX.md` 작성 (3-part 구조 설명 + 기존 3 세션 이관 placeholder) | sessions-index | ✅ |
| 5 | `learning-logs/_INDEX.md` 작성 (패턴 후보 3건 명세) | learning-index | ✅ |
| 6 | `learning-logs/_TEMPLATE.md` 작성 (frontmatter visibility 필수) | learning-template | ✅ |
| 7 | `.visibility-rules.yaml` 작성 (workflow-v2-design §9.4 기반 + HANDOFF/BRIEFING/PROGRESS/WORK-LOG 추가 규칙) | visibility-rules | ✅ |
| 8 | `PROGRESS.md` frontmatter 에 `current_wu` / `current_wu_path` 추가 + `last_overwrite` 갱신 | progress-frontmatter | ✅ |
| 9 | `scripts/snapshot.sh` 작성 (workflow-v2-design §10.6 기반 + 2-step FUSE/Local 저장) | snapshot-sh | ✅ |
| 10 | `scripts/squash-wu.sh` 작성 (interactive rebase + FUSE bypass 자동) | squash-wu-sh | ✅ |
| 11 | `.gitignore` 에 `sprints/_scratch/` 추가 (tmp/snapshots/ 는 상위 tmp/* 룰이 이미 차단) | gitignore-wu15 | ✅ |
| 12 | 본 `sprints/WU-15.md` 작성 | wu15-file | ✅ |
| 13 | 전체 staging + 단일 커밋 `WU-15: <title>` | wu15-commit | ✅ `aa0a354` |

> **주의**: WU-15 는 first-try 단일 커밋 (micro-wip 커밋 분할 없음). 사유 — 인프라 파일 간 의존 관계가 얕고, 전체를 하나의 atomic 상태로 확정하는 편이 "v2 시작점" 증적으로 유리. 이후 WU-16+ 부터 wip → squash 패턴 정상 적용.
>
> **FUSE bypass 1회 적용**: stale `.git/index.lock` (2026-04-20 05:42, 0 bytes) 감지 → `cp -a .git /tmp/solon-git-20260421T021950/` → `unset GIT_DIR` 경유 → `git add -A` + `git commit` → `rsync back` → cleanup. FUSE mount 의 stale lock 은 rsync --delete 로도 제거 불가 (권한 문제) 이나 working tree clean · HEAD 정상 확인으로 무해 판정.

## 3. Acceptance (완료 조건)

- [x] CLAUDE.md v1.15 — §14.3 제거 · §14 헤더 role 명시
- [x] `sprints/` + `sessions/` + `learning-logs/` 디렉토리 + 각 `_INDEX.md` 존재
- [x] `learning-logs/_TEMPLATE.md` 스켈레톤 완성
- [x] `.visibility-rules.yaml` — 3-tier 규칙 + tier_resolver + override_replace 명세
- [x] `scripts/snapshot.sh` — 2-step (FUSE + /tmp) 저장 + manifest + 24h cleanup
- [x] `scripts/squash-wu.sh` — FUSE bypass 자동 감지 + dry-run 모드
- [x] `.gitignore` — `sprints/_scratch/` 추가
- [x] `PROGRESS.md` frontmatter — `current_wu` / `current_wu_path` 추가
- [x] `sprints/WU-15.md` (본 파일) 작성
- [x] **전체 단일 커밋** `WU-15: Workflow v2 인프라 설정` 완료 (`aa0a354`, FUSE bypass 1회 적용)
- [x] Solon Status Report v0.6.3 출력 (§14 code fence, WU-15 완료 dashboard)
- [→] PROGRESS.md `① Just-Finished` 에 final sha 반영 — **WU-15.1 refresh 에서 처리**

## 4. Out-of-scope (다음 WU 로 이관)

- **WU-16**: 기존 WU-7 ~ WU-14.1 의 `sprints/WU-<id>.md` 독립 파일 이관 + 과거 3 세션 `sessions/<date>-<codename>.md` 3-part 로그 재구성.
- **WU-17**: HANDOFF-next-session.md + NEXT-SESSION-BRIEFING.md 를 `sprints/_INDEX.md` + `sessions/_INDEX.md` 참조 구조로 축소 (-80%).
- **WU-18**: v2 운영 1주 검증 + learning-logs/ 첫 패턴 3건 실체화 (`P-fuse-git-bypass`, `P-compact-recovery`, `P-two-step-wu-refresh`) + 임계값 조정 여부.

## 5. Decision log

- 본 WU 내 사용자 결정 **0건**. §14.3 수용은 WU 착수 전 대화에서 종결.
- `.gitignore` 수정: `tmp/snapshots/` 는 기존 `tmp/*` 룰이 이미 차단하므로 **중복 선언 생략** → `sprints/_scratch/` 만 추가. (원래 계획과 다름 — 사용자 확인 없이 결정한 건이지만 원칙 2 위반 아님: 사실 관계 기반 기술 결정, 동작 동일.)
- `NEXT-SESSION-BRIEFING.md` dangling change 흡수: 이전 세션에서 W10 사전 분석 3건 참조 2-line 추가분이 커밋 누락 상태. WU-17 에서 BRIEFING 전체 축소 예정이므로 WU-15 atomic state 에 함께 흡수. (기술 cleanup, 원칙 2 무관.)

## 6. Post-commit 체크리스트

1. `git log --oneline -5` 로 WU-15 final sha 확인.
2. frontmatter `final_sha` + `closed_at` + `status: done` 반영 (→ 이게 WU-15.1 refresh 사유가 됨).
3. PROGRESS.md `① Just-Finished` + `current_wu` (이제 WU-16 or null) 갱신.
4. 필요 시 `scripts/snapshot.sh "event:wu-transition"` 1회 실행 — smoke test.
5. Solon Status Report v0.6.3 출력 (WU-15 완료 리포트).
6. `git push` 는 **사용자 터미널** 에서만 (§1.5).
