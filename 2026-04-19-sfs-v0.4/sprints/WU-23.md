---
wu_id: WU-23
title: "#1 sfs slash command detail design (6 명령 minimal contract spec)"
status: done
opened_at: 2026-04-25T17:37:22+09:00
closed_at: 2026-04-25T18:00:00+09:00
session_opened: trusting-stoic-archimedes
session_closed: trusting-stoic-archimedes
final_sha: 1e0e6f1   # 21번째 세션 trusting-stoic-archimedes 가 close commit (TBD_21ST_CLOSE → 1e0e6f1, full: 1e0e6f1...). step-2 commit. 사용자 복귀 후 squash 시 refresh WU-23.1 로 갱신 가능 (선택).
refresh_wu: null
visibility: business-only
entry_point: |
  WU-22 §3 β (themed-bundles) 채택에 따라, 0.3.0-mvp 번들의 핵심 #1 (sfs slash command 실제 logic) 의
  **detail design only** WU. 구현은 WU-24~26 에서 분할.
  사용자 결정 WU22-D2 = "6개 명령 전부" (status/start/plan/review/decision/retro) — 21번째 세션 진입 시 명시.
  본 WU 는 6개 명령 각각의 minimal contract spec (input/output/side-effect/error-cases) 1차안 + 3-agent vote.
depends_on_reads:
  - 2026-04-19-sfs-v0.4/CLAUDE.md (§1 13 규율, §3 디렉토리 구조, §7 visibility tier)
  - 2026-04-19-sfs-v0.4/PROGRESS.md (frontmatter mutex + ② In-Progress)
  - 2026-04-19-sfs-v0.4/sprints/WU-22.md (β release grouping 결정 근거)
  - ~/workspace/solon-mvp/templates/.claude/commands/sfs.md (어댑터 정의 — 현 상태 확인)
  - ~/workspace/solon-mvp/templates/.sfs-local/ (events.jsonl 등 현 상태)
files_touched:
  - 2026-04-19-sfs-v0.4/sprints/WU-23.md
  - 2026-04-19-sfs-v0.4/PROGRESS.md (frontmatter current_wu + heartbeat)
  - 2026-04-19-sfs-v0.4/sprints/_INDEX.md (활성 WU 섹션)
decision_points:
  - todo_id: WU22-D2
    type: sfs-slash-scope
    escalated_to: 사용자 (resolved)
    options: "MVP 6개 명령 (status/start/plan/review/decision/retro) 전부 vs 핵심 2-3개 우선"
    resolved: 2026-04-25T17:00:00+09:00
    resolution: "6개 명령 전부 (사용자 21번째 세션 진입 시 명시)"
    resolution_session: trusting-stoic-archimedes
  - todo_id: WU23-D1
    type: contract-spec-schema
    escalated_to: 3-agent vote (CEO+CTO+CPO)
    options: "본 WU §1 의 6개 명령 minimal contract spec 1차안 채택 여부"
    resolved: 2026-04-25T17:55:00+09:00
    resolution: "PASS (V-1 outcome, 3/3 effective PASS, 3 conditions applied)"
    vote_record: "본 WU §3 V-1 참조 (CEO/CTO/CPO 각자 verbatim 기록)"
  # ── 22nd 세션 §7 일괄 resolved (사용자 `adoring-trusting-feynman` 7항목 결정) ──
  - todo_id: WU22-D3
    type: gate-id-schema-location   # §7.1 #1 (block)
    escalated_to: 사용자 (resolved)
    options: "(a) CLAUDE.md §3.1 또는 00-intro.md inline / (b) sfs.md adapter 내부 / (c) 별도 spec 파일 신설"
    resolved: 2026-04-25T22:55:00+09:00
    resolution: "(c) 별도 spec 파일 신설 — `gates.md` 신설 완료 (SSoT pointer = 05-gate-framework.md, 7-gate enum + verdict enum + sfs CLI 매핑 + WU-23 §1.4 draft 정정 기록). 동시에 메타 규칙 §1.14 (CLAUDE.md ≤200 lines) 신설."
    resolution_session: adoring-trusting-feynman
    impacts: "WU-25 (`/sfs review`) 구현 시 valid enum = G-1, G0, G1, G2, G3, G4, G5 (gates.md §3 형식 사용). WU-23 §1.4 stderr `valid: G0/G1/G2/G4` draft 는 정정 대상."
  - todo_id: WU22-D4
    type: sfs-status-output-format   # §7.2 #2
    escalated_to: 사용자 (resolved)
    options: "구분자 (· vs | vs space), 색상 on/off, last_event ISO8601 vs human"
    resolved: 2026-04-25T22:55:00+09:00
    resolution: "`·` 구분자 + 색상 on/off CLI flag (`--color=auto/always/never`) + ISO8601 (사람 가독 + CI 친화 모두 충족)"
    resolution_session: adoring-trusting-feynman
    impacts: "WU-24 (`/sfs status`) 구현 시 `--color` flag 분기 추가."
  - todo_id: WU22-D5
    type: sprint-id-auto-naming   # §7.2 #3
    escalated_to: 사용자 (resolved)
    options: "(a) ISO week (`<YYYY-W>-sprint-<N>`) / (b) date / (c) 둘 다"
    resolved: 2026-04-25T22:55:00+09:00
    resolution: "(a) ISO week — docset 8건 일관 사용 중 (PHASE1-KICKOFF-CHECKLIST.md / phase1-mvp-templates/ / solon-mvp-dist/templates/ 등). β default 권장 받아들임."
    resolution_session: adoring-trusting-feynman
    impacts: "WU-24 (`/sfs start`) 구현 시 sprint-id pattern = `<YYYY-W>-sprint-<N>`."
  - todo_id: WU22-D6
    type: editor-auto-launch   # §7.2 #4
    escalated_to: 사용자 (resolved)
    options: "(a) `$EDITOR <file>` auto-launch / (b) 단순 파일 준비만"
    resolved: 2026-04-25T22:55:00+09:00
    resolution: "(b) 단순 파일 준비만 — IDE/Cursor 호환성, CI 환경 hang 회피."
    resolution_session: adoring-trusting-feynman
    impacts: "WU-25 (`/sfs plan`/`review`) + WU-26 (`/sfs retro`) 모두 파일 path stdout 출력만, 에디터 launch 안 함."
  - todo_id: WU22-D7
    type: mini-adr-format   # §7.2 #5
    escalated_to: 사용자 (resolved)
    options: "(a) 표준 ADR 5섹션 / (b) W10 TODO 압축형 / (c) 하이브리드"
    resolved: 2026-04-25T22:55:00+09:00
    resolution: "(c) 하이브리드 — frontmatter (todo_id, found_in_wu, status, tier) + 5 섹션 (Context / Decision / Alternatives / Consequences / References). W10 TODO 19항목 마이그레이션 가능 + 표준 ADR 호환."
    resolution_session: adoring-trusting-feynman
    impacts: "WU-29 (`/sfs decision` mini-ADR template) 구현 시 본 형식 사용. cross-ref-audit §4 W10 TODO 19항목 후속 마이그레이션 여지 보존."
  - todo_id: WU22-D8
    type: sfs-retro-close-auto-commit   # §7.2 #6
    escalated_to: 사용자 (resolved)
    options: "(a) auto commit / (b) no auto commit"
    resolved: 2026-04-25T22:55:00+09:00
    resolution: "(a) auto commit — PROGRESS.md 가 히스토리 보유, 사용자가 직접 git add/commit 하는 번거로움 회피. §1.5 push 금지는 그대로 유지 (commit auto, push manual)."
    resolution_session: adoring-trusting-feynman
    impacts: "WU-26 (`/sfs retro --close`) 구현 시 sprint close 후 auto `git add` + `git commit` (commit message format 후속 결정). push 는 사용자 터미널."
  - todo_id: WU22-D9
    type: f-04-fix   # §7.5 (escalate 였던 항목)
    escalated_to: 사용자 (resolved)
    options: "7.5.1 정규식 (i/ii/iii/iv) + 7.5.2 검증기 (a mode flag / b 두 파일 분리)"
    resolved: 2026-04-25T22:55:00+09:00
    resolution: "7.5.1 = (i) `[A-Z]{2,}` minimal regex (사용자 위임). 7.5.2 = (b) 두 검증기 분리 (`verify-install.sh` + `verify-w0.sh`). WU-30 신설로 분리."
    resolution_session: adoring-trusting-feynman
    impacts: "WU-30 (F-04 fix) 신설. WU-21 §F-04 cross-link. R-D1 영향 0 (정규식만 + 새 파일)."
learning_patterns_emitted:
  - "P-04 후보 (session-hang takeover, WU-22 §7 근거) — 21번째 세션이 P-04 별도 WU 에서 실체화 가능"
  - "P-05 후보 (agent_loader_startup_only) — Claude Code 의 .claude/agents/ 가 mid-session reload 안 됨, fallback A 패턴 = general-purpose + 페르소나 .md Read 지시. 사용자 복귀 후 재시작 시 native alt B 활성"
  - "P-06 후보 (claude-md-line-limit-meta-rule, 22nd 세션) — CLAUDE.md ≤200 lines 메타 규칙 신설로 SSoT 분량 유지 패턴. 첫 사례 = §14 → solon-status-report.md 분리. visibility raw-internal."
sub_steps_split: false   # 200 line 임계 도달 시 true 로 전환
---

# WU-23 — #1 sfs slash command detail design (6 명령 minimal contract spec)

> **Context**: WU-22 §3 β 채택 → 0.3.0-mvp = #1 + #4 + #6 번들. 본 WU 는 #1 의 **detail design only** (구현은 WU-24~26).
> **사용자 결정 WU22-D2**: "6개 명령 전부" — 21번째 세션 진입 시 명시.
> **합의 protocol**: 본 WU 는 의미적 결정 포함이므로 3-agent (CEO+CTO+CPO) 합의 + 2/3 다수결 적용.
> **Session note**: 21번째 세션 `trusting-stoic-archimedes` 가 사용자 부재 4시간 동안 자율 진행. 페르소나 alt B 등록 완료, 실 호출은 fallback A (general-purpose + 페르소나 spec Read 지시) 로 전환 (Claude Code agent loader 가 startup-only 라 mid-session reload 안 됨, 사용자 복귀 후 재시작 시 native alt B 활성).

---

## §0. 작업 진행 원칙 (본 WU 한정)

1. **MINIMAL contract** — 각 명령의 "최소 무엇을 하는가" 만 정의. 구체 출력 포맷 / 색상 / 이모지 등 product decision 은 ⚠️ marker 후 사용자 복귀 대기.
2. **3-agent vote** — §1 contract draft 1차안 완성 후 CEO/CTO/CPO 각자 1표. 2/3 PASS → §3 에 결정 기록 + 다음 단계. 2/3 FAIL → 1 retry round. 2nd FAIL → ⚠️ escalate.
3. **사실 작업 vote skip** — 단순 frontmatter 갱신, sha backfill, typo 수정은 sole-author OK (planner.md "When to SKIP 3-agent Vote" 참조).
4. **R-D1 준수** — `~/workspace/solon-mvp/` 는 read-only 로 참조만. 본 WU 의 산출물은 docset 안에만 (구현은 후속 WU 가 templates/scripts/ 에 반영).
5. **§1.5 push 금지** — 21번째 세션은 사용자 부재. local commit 까지만, push 는 사용자 복귀 후 본인 터미널.

---

## §1. 6개 명령 minimal contract spec (1차 draft, vote 대기)

> 이 §1 의 spec 은 **CEO 1차 draft** 입니다. CTO 가 검토 + 보완 + 자체 vote, CPO 가 5축 평가 + vote, CEO 자체 vote. 3 vote 합산 → 2/3 PASS 면 본 §1 확정 + §3 에 vote_record 기록.

### 1.1 `/sfs status` — 현 상태 1줄 요약 출력

| 항목 | 내용 |
|---|---|
| **Goal** | 현재 sprint / WU / gate 상태를 1줄 요약 (CLI 응답 + 사용자 대시보드 용도) |
| **Input** | (none) — `.sfs-local/` 디렉토리 자동 탐색 |
| **Output (stdout)** | 1 줄 텍스트: `sprint <id> · WU <wu_id> · gate <last_gate>:<verdict> · ahead <N> · last_event <ts>` |
| **Side-effect** | 없음 (read-only) |
| **Error cases** | (a) `.sfs-local/` 부재 → exit 1, stderr "no .sfs-local found, run /sfs start first". (b) `events.jsonl` 손상 → exit 2, stderr "events.jsonl parse error at line <n>". (c) git repo 아님 → exit 3, stderr "not a git repo (sfs requires git for ahead count)". |
| **Exit code** | 0 success / 1 no init / 2 corrupt / 3 no-git / 99 unknown |
| **⚠️ pending product decisions** | 정확한 출력 포맷 (구분자: `·` vs `\|` vs space)? 색상 사용 여부? `last_event` ISO8601 vs 사람 읽기 좋은 포맷? → 사용자 결정 필요 (escalate marker) |

### 1.2 `/sfs start` — 새 sprint 초기화 (= scaffold dir + 4 templates)

| 항목 | 내용 |
|---|---|
| **Goal** | 새 sprint 디렉토리 + 4 파일 (plan/log/review/retro) 자동 scaffold. #6 Sprint cycle CLI helper 와 통합. |
| **Input** | `<sprint-id>` (positional, optional). 미지정 시 자동 생성: `<YYYY-W>-sprint-<N>` (W=ISO week, N=주 안의 sprint 번호). |
| **Output (stdout)** | 다중 줄: `created: sprints/<sprint-id>/` + 생성 파일 4개 list. |
| **Side-effect** | (a) `sprints/<sprint-id>/` 디렉토리 생성. (b) `plan.md`/`log.md`/`review.md`/`retro.md` 4 파일 templates 복사. (c) `.sfs-local/events.jsonl` 에 entry append (`{"ts":..., "type":"sprint_start", "sprint_id":..., "by":"sfs-start"}`). |
| **Error cases** | (a) 동명 sprint 존재 → exit 1, stderr "sprint <id> already exists, use --force or pick another id". (b) templates 부재 (`templates/sprint-templates/` not found) → exit 4, stderr. (c) 권한 부족 → exit 5. |
| **Exit code** | 0 success / 1 conflict / 4 templates-missing / 5 permission / 99 unknown |
| **⚠️ pending product decisions** | sprint-id auto naming: ISO week (`2026-W17-sprint-1`) vs date (`2026-04-25-sprint-1`)? 사용자 docset 의 기존 sprint 명명 규칙과 정합 필요. |

### 1.3 `/sfs plan` — 현 sprint 의 plan.md 진입 (open / scaffold / append)

| 항목 | 내용 |
|---|---|
| **Goal** | 현 sprint 의 `plan.md` 를 사용자 편집 진입 상태로 만든다. 미존재 시 template 으로 신설. 존재 시 frontmatter `phase: plan` 갱신. |
| **Input** | (none — 현 sprint 컨텍스트는 `.sfs-local/current-sprint` 에서 추출) |
| **Output (stdout)** | `plan.md ready: <path>` 1 줄. (실제 편집은 사용자 에디터 책임 — `/sfs plan` 은 파일 준비만). |
| **Side-effect** | (a) `plan.md` 미존재면 신설 (template 적용). (b) frontmatter `phase: plan` + `last_touched_at` 갱신. (c) `events.jsonl` append (`{"type":"plan_open", ...}`). |
| **Error cases** | (a) 활성 sprint 없음 → exit 1, stderr "no active sprint, run /sfs start first". (b) `current-sprint` 파일 손상 → exit 2. |
| **Exit code** | 0 / 1 no-sprint / 2 corrupt / 99 |
| **⚠️ pending product decisions** | 명령이 에디터 자동 launch 해야 하는가 (`$EDITOR plan.md`) vs 단순 파일 준비만? 사용자 워크플로 (vim / VS Code / Cursor) 에 따라 다름. |

### 1.4 `/sfs review` — 현 sprint review (gate verdict 기록 진입)

| 항목 | 내용 |
|---|---|
| **Goal** | 현 sprint 의 `review.md` 작성 진입 + gate verdict 기록 가능 상태로 만든다. |
| **Input** | (optional) `--gate <gate-id>` (예: G2 / G4) — verdict 기록 대상 gate. 미지정 시 마지막 active gate. |
| **Output (stdout)** | `review.md ready: <path> | gate <gate-id> awaiting verdict`. |
| **Side-effect** | (a) `review.md` frontmatter `phase: review` + `gate_id` 갱신. (b) `events.jsonl` append (`{"type":"review_open", "gate_id":..., ...}`). |
| **Error cases** | (a) 활성 sprint 없음 → exit 1. (b) 알 수 없는 gate-id → exit 6, stderr "unknown gate <id>, valid: G0/G1/G2/G4". |
| **Exit code** | 0 / 1 no-sprint / 6 unknown-gate / 99 |
| **⚠️ pending product decisions** | **CTO+CPO 공동 지적**: gate id schema (G0/G1/G2/G4 정의 위치) 가 docset 본문 (`00-intro.md` 또는 `CLAUDE.md §3` 등) 에 이미 정의돼 있는지 검증 필수. WU-24 구현 진입 전 사용자 확정 필요 — 부재 시 본 명령이 정의해야 함 (별도 의미 결정, ⚠️ escalate). 현 draft 는 예시만 (G0/G1/G2/G4). |

### 1.5 `/sfs decision` — mini-ADR 신설

| 항목 | 내용 |
|---|---|
| **Goal** | `decisions/<id>-<kebab>.md` mini-ADR 자동 생성 (#5 ADR helper 와 통합 — 0.3.x patch). |
| **Input** | `<title>` (positional, required) — 결정 1줄 제목. (optional) `--id <override-id>` — id 수동 지정. |
| **Output (stdout)** | `decision created: decisions/<id>-<kebab>.md` 1 줄. |
| **Side-effect** | (a) `decisions/` 디렉토리 (없으면 생성) + 새 ADR 파일 (frontmatter + 5 섹션 template). id 자동 부여 (가장 큰 기존 id + 1, 시작은 `0001`). (b) `events.jsonl` append (`{"type":"decision_created", "decision_id":..., ...}`). |
| **Error cases** | (a) `<title>` 미지정 → exit 7, stderr usage. (b) `--id` 충돌 → exit 1, stderr "decision id <id> already exists". (c) template 부재 → exit 4. |
| **Exit code** | 0 / 1 conflict / 4 template-missing / 7 missing-arg / 99 |
| **⚠️ pending product decisions** | mini-ADR 템플릿 5 섹션 = (Context / Decision / Alternatives / Consequences / References) — 사용자 docset 의 cross-ref-audit §4 W10 TODO 형식과 정합 여부 확인 필요. |

### 1.6 `/sfs retro` — 현 sprint retrospective 진입

| 항목 | 내용 |
|---|---|
| **Goal** | 현 sprint 의 `retro.md` 작성 진입 + sprint close 준비. |
| **Input** | (optional) `--close` — retro 작성 후 sprint 을 `closed` 상태로 마킹. |
| **Output (stdout)** | `retro.md ready: <path>` (+ `sprint closed: <id>` if `--close`). |
| **Side-effect** | (a) `retro.md` frontmatter `phase: retro` 갱신. (b) `--close` 시 `current-sprint` 클리어 + sprint frontmatter `status: closed` + `closed_at`. (c) `events.jsonl` append. |
| **Error cases** | (a) 활성 sprint 없음 → exit 1. (b) `--close` 인데 review.md 미작성 → exit 8, stderr "review.md required before close (run /sfs review first)". |
| **Exit code** | 0 / 1 no-sprint / 8 review-missing / 99 |
| **⚠️ pending product decisions** | `--close` 가 자동 git commit 도 해야 하는가? §1.5 (no auto push) 와 별개로 commit 자동화는 사용자 워크플로 결정 필요. |

---

## §2. 공통 모듈 / 의존성

- **`.sfs-local/current-sprint`** — 현 활성 sprint id 1줄 텍스트 (utf-8, trailing newline). naming pattern 예시: `<YYYY-W>-sprint-<N>` (ISO week 기반, 사용자 docset 기존 관례 정합 확인 필요 — pending decision). 미존재 = no active sprint.
- **`.sfs-local/events.jsonl`** — append-only event log. 모든 명령이 entry 1개 추가 (#4 schema 표준화는 별도 WU 에서 확정 — 25th-1 재배정 후 WU 번호 미정, 본 자리는 WU-27 = `/sfs loop`).
- **공통 helper** — `sfs-common.sh`: (a) `read_current_sprint()`, (b) `append_event()`, (c) `validate_sfs_local()`, (d) error exit helper.
- **외부 의존성** — git (status/log/rev-list), jq (events.jsonl 읽기/쓰기 — 권장 but optional). bash 4+.

---

## §3. Vote Record (3-agent 합의 결과)

> 이 §3 은 vote 이벤트마다 append-only. 1 vote 이벤트 = 1 sub-section (### V-1 / V-2 ...).

### V-1 — §1 contract spec 1차안 채택 여부 (✅ PASS, with 3 conditions applied)

- **target**: 본 WU §1 (6 명령 contract spec) 1차 draft (CEO 작성).
- **invoked_at**: 2026-04-25T17:50:00+09:00 (21번째 세션 trusting-stoic-archimedes, 사용자 부재 자율 작업 mode).
- **invocation_method**: fallback A — `Agent(subagent_type="general-purpose")` + 페르소나 .md Read 지시 (Q2 alt B 의 native 호출은 mid-session reload 안 됨, 사용자 복귀 후 Claude Code 재시작 시 native 활성 예상).

#### Vote Record (verbatim)

**CTO Vote** (general-purpose + Read `.claude/agents/generator.md`):
```
verdict: CONDITIONAL
score: 83
rationale: §1 contract spec 은 구조·error-cases·exit-code 완전. 6개 명령 minimal 정의 우수.
           2개 이슈: (a) gate id schema location 확인 필수 (WU-24 block),
           (b) `.sfs-local/current-sprint` naming pattern spec 부실. 둘 다 2-line fix.
conditions:
  - (CTO-1) WU-23 §1.4 pending decision 항목에 gate id location verification CTO note 추가.
  - (CTO-2) WU-23 §2 `.sfs-local/current-sprint` 에 naming pattern 예시 추가.
risks_flagged:
  - WU-24 구현 단계에서 gate id 미검증 시 `/sfs review` logic 복잡해짐.
  - `--close` 시 git commit 자동화 여부는 §1.5 (no auto push) 와 정합적으로 "no auto commit" 권장.
```

**CPO Vote** (general-purpose + Read `.claude/agents/evaluator.md`):
```
verdict: CONDITIONAL
total_score: 90
axis_scores: { fact: 90, rule: 95, ssot: 92, risk: 85, repro: 88 }
rationale:
  fact: Gate id schema 출처 미검증 (CTO 도 이슈, WU-25 영향 가능). 6 명령 contract 구조 명확. 90점.
  rule: §1 13 규율 + §5 frontmatter 완벽 준수. Escalation marker 적절. 95점.
  ssot: PROGRESS/WU-23 정합 높음. WU-22 β → WU-23 §1 이음 entry_point 에만 기술. 92점.
  risk: Pending decisions 3개 = 2-line fix 수준, 구현 블록 아님. 85점.
  repro: Vote format/frontmatter 재현 가능, 구현 idempotency 후속 WU 담당. 88점.
conditions:
  - (CPO-1) WU-24 구현 단계 시작 전 gate id 정의 위치 확정 — CTO 검증 노트 추가.
  - (CPO-2) WU-23 §1.4 pending decision 항목에 "별도 의미 결정" 1줄 추가.
  - (CPO-3) Pending decisions 3개 (sprint-id auto naming, editor auto-launch, --close auto commit)
            구현 단계 진입 시 사용자 결정 대기로 이관 (escalation point 기록).
risks_flagged:
  - Gate id schema 미검증 → WU-25 역참조 필요.
  - Pending decisions 3개 중 sprint-id 는 사용자 docset 기존 관례와 정합 필수.
  - Conditional 조건 모두 2-line 수준 fix, PASS 가능성 95% 이상.
escalation_recommended: no
```

**CEO Vote** (Claude main session, trusting-stoic-archimedes):
```
verdict: CONDITIONAL
score: 88
rationale: §1 1차 draft 는 사용자 결정 WU22-D2 (6개 전부) 와 일치, minimal contract 원칙 준수.
           CTO+CPO 가 공통 지적한 gate id location 이슈 + naming pattern + pending decisions
           escalation 모두 합리적. 본 draft 의 의미적 결정 핵심 (6 명령 scope, side-effect 시맨틱)은
           CEO 관점에서도 PASS. CTO/CPO 의 3 conditions 통합 적용 후 §1 확정 진행.
conditions:
  - (CEO-1) CTO+CPO 의 conditions 3개 모두 통합 적용 (CTO-1+CPO-2 = §1.4 보강, CTO-2 = §2 보강, CPO-3 = §7 신설).
escalation_recommended: no  (단 §7 에 사용자 복귀 시 결정 대기 항목 정리)
```

#### Tally Result

| Voter | Verdict | Score | Effective verdict (CONDITIONAL → PASS only if ≤2-line fixes) |
|---|:---:|:---:|:---:|
| CEO | CONDITIONAL | 88 | PASS (1 condition, all 2-line fix) |
| CTO | CONDITIONAL | 83 | PASS (2 conditions, all 2-line fix) |
| CPO | CONDITIONAL | 90 | PASS (3 conditions, all 2-line fix) |

**Effective tally**: 3/3 PASS (≥ 2/3 majority requirement) → **OUTCOME = PASS**.

#### Conditions Applied (3 통합)

1. ✅ **(CTO-1 + CPO-2 통합)** — WU-23 §1.4 pending decision 항목 보강 (gate id location verification + "별도 의미 결정" 명시).
2. ✅ **(CTO-2)** — WU-23 §2 `.sfs-local/current-sprint` naming pattern 예시 추가 (`<YYYY-W>-sprint-<N>` ISO week, utf-8, trailing newline).
3. ✅ **(CPO-3)** — WU-23 §7 신설: 사용자 복귀 시 결정 대기 항목 4개 정리 (gate id location, sprint-id auto naming, editor auto-launch, --close auto commit).

#### Dissent / Escalation Record

- 모든 vote 가 CONDITIONAL → 의견 일치도 매우 높음. 별도 dissent 기록 없음.
- escalation_recommended = CTO yes(implicit, gate id), CPO no, CEO no(단 §7 에 정리). → §7 신설로 통일 처리.
- 본 vote 는 fallback A 호출 (Q2 alt B 의 native 호출 안 됨). 사용자 복귀 후 Claude Code 재시작 시 native subagent_type=generator/evaluator 작동 예상 — V-2 부터 native 호출 시도 권장.

#### V-1 close

V-1 종결: **2026-04-25T17:55:00+09:00**. §1 confirmed (3 conditions applied below). 다음 단계 = §1 의 conditions 적용 마무리 + §7 사용자 복귀 시 결정 대기 신설 + WU-23 close 준비.

---

## §4. Sub-step 분할 / 후속 WU 매핑 (β roadmap 일부)

본 WU 종료 후 후속:

- **WU-24** — #1 구현 part 1: `/sfs status` + `/sfs start` (events.jsonl write 첫 도입, sprint scaffold)
- **WU-25** — #1 구현 part 2: `/sfs plan` + `/sfs review` (gate verdict 통합)
- **WU-26** — #1 구현 part 3: `/sfs decision` + `/sfs retro` (mini-ADR + close)
- **WU-27** — `/sfs loop` (Ralph Loop + Solon mutex + Solon-wide executor convention 신규 도입), 24th-52 결정 #4 trigger, 25th-1 admiring-zealous-newton 신설 ※ 원래 자리 (#4 events.jsonl schema 표준화) 는 별도 WU 로 재배정 — cross-ref-audit.md §4 W-20 TODO 참조
- **WU-28** — #6 Sprint cycle CLI helper (`/sfs start` 보강 + sprint dir 4 템플릿) ※ 25th-1 재배정 후 #4 events.jsonl schema 표준화 와 번호 충돌 — 후속 cycle 에서 사용자 결정 영역 (W-20)
- **WU-29** — #5 ADR helper (`/sfs decision` 후처리, 0.3.x patch)
- **0.3.0-mvp cut** — WU-23~28 묶음 완료 시점 (β bundle, 25th-1 재배정 영향 적음 — 본 묶음은 0.3.0 시점에 클로즈됨).

---

## §5. 본 WU 진행 체크리스트

- [x] WU-23 frontmatter 작성 (decision_points WU22-D2 resolved=6개 전부)
- [x] §1 6 명령 contract spec 1차 draft (CEO 작성)
- [x] CTO sub-agent invoke → CONDITIONAL vote (score 83, 2 conditions)
- [x] CPO sub-agent invoke → CONDITIONAL vote (total 90, 3 conditions)
- [x] CEO 자체 vote 기록 (CONDITIONAL, score 88)
- [x] §3 V-1 vote_record append (3 votes + outcome PASS)
- [x] §1 의 3 conditions 적용 (§1.4 + §2 + §7 신설)
- [x] §7 사용자 복귀 시 결정 대기 신설 (4-block + product decisions + non-block 분류)
- [x] WU-23 frontmatter status=done, closed_at, session_closed=trusting-stoic-archimedes
- [ ] PROGRESS.md ②/③ 갱신 + sprints/_INDEX.md row 이동 (close → 완료 섹션) — 다음 micro-step
- [x] close commit `1e0e6f1` + final_sha backfill (TBD_21ST_CLOSE → 1e0e6f1) — 21번째 세션 자율 진행
- [ ] 사용자 복귀 후 push (§1.5)

---

## §6. 결론 / 다음 발화

✅ **§1 6 명령 contract spec 확정** (V-1 vote PASS, 3 conditions applied).
⏭️ **다음 = WU-23 close 준비** + WU-24 entry 준비. 단 WU-24 entry 는 사용자 복귀 후 (gate id schema 확정 결정 선행 필요 — §7 참조).

본 WU 는 contract spec 만 다룬다. 실제 명령 구현 (bash logic + handler 분기) 은 WU-24~26.

---

## §7. 사용자 복귀 시 결정 대기 (⚠️ escalation point)

> 본 §7 은 21번째 세션 자율 작업 중 발생/식별된 의미적 결정을 한 곳에 모아 **사용자 복귀 시 일괄 처리 가능** 하게 한다. CPO-3 condition 충족.

### 7.1 WU-24 구현 진입 전 결정 필요 (block)

| # | 항목 | 영향 | 권장 옵션 |
|:-:|---|---|---|
| **1** | **Gate id schema 정의 위치** | WU-25 (`/sfs review` gate verdict) 직접 영향 | (a) `00-intro.md` 또는 `CLAUDE.md §3` 에 G0/G1/G2/G4 명시 / (b) sfs.md adapter 안에서 정의 / (c) 별도 spec 파일 신설 |

### 7.2 구현 단계 product decisions (각 명령별)

| # | 명령 | 결정 항목 | 권장 default |
|:-:|---|---|---|
| **2** | `/sfs status` | 출력 포맷 — 구분자 (`·` vs `\|` vs space), 색상 사용, `last_event` ISO8601 vs human-readable | `·` 구분자, 색상 off (CI 친화), ISO8601 |
| **3** | `/sfs start` | sprint-id auto naming — ISO week (`2026-W17-sprint-1`) vs date (`2026-04-25-sprint-1`) | ISO week (사용자 docset 기존 관례 확인 필요) |
| **4** | `/sfs plan` / `review` | 에디터 auto-launch (`$EDITOR <file>`) vs 단순 파일 준비 | 단순 준비만 (`$EDITOR` 없는 환경 호환) |
| **5** | `/sfs decision` | mini-ADR 5섹션 = (Context / Decision / Alternatives / Consequences / References) — W10 TODO 형식 정합 | W10 TODO 형식과 호환되게 align (cross-ref-audit §4 참조) |
| **6** | `/sfs retro` | `--close` 시 git commit 자동화 여부 | **no auto commit** (§1.5 no auto push 정신 일치) |

### 7.3 후속 의사 결정 (non-block, 정보)

- 본 vote (V-1) 의 fallback A 사용 = Q2 alt B (`.claude/agents/` 정식 등록) 의 mid-session reload 미작동 결과. 사용자 복귀 후 Claude Code 재시작 시 native subagent_type=generator/evaluator 작동 예상. V-2 부터 native 호출 시도 권장 (학습 패턴 P-04 와 별개로 P-05 후보 = "agent_loader_startup_only" 패턴).
- WU-23 의 visibility tier `business-only` 선언은 .visibility-rules.yaml scope (현 `2026-04-19-sfs-v0.4/` 한정) 와 별개. `.claude/agents/` 가 `agent_architect/` 루트에 있어 기존 visibility 규칙 미적용 — 후속 WU 에서 .visibility-rules.yaml scope 확장 결정 필요 (의미 결정).

### 7.5 F-04 verify-w0.sh false-positive 2건 (21번째 세션 fix 시도 → escalate)

WU-21 §F-04 발견 후 21번째 세션이 fix 시도 → **사용자 결정 의존** 으로 ⚠️ escalate 처리:

| # | 항목 | 분석 | 사용자 결정 필요 |
|:-:|---|---|---|
| **7.5.1** | check #6 placeholder 정규식 over-match (`<N>`, `<YYYY-W>` 등 설명용도 매치) | 현 `'<[A-Z][A-Z0-9_-]*>'` 패턴이 1자/short 설명용까지 매치. fix 옵션 (i) 최소 길이 (`[A-Z]{2,}`) — `<DB>` 도 차단 / (ii) `_` only separator — 사용자 관례 의존 / (iii) whitelist — 유지보수 코스트 / (iv) marker 변경 (`{{STACK}}`) — install.sh templates 큰 변경 | 사용자 docset 의 placeholder 관례 (separator: `_` vs `-`) 확정 필요 |
| **7.5.2** | check #7 install.sh output over-strict (OSS 제품명 'Solon' 차단) | verify-w0.sh 가 setup-w0.sh 전용인지 (mode flag) vs 두 target 분리 (verify-install.sh + verify-w0.sh) 결정 | 두 검증기 분리 vs mode flag — 아키텍처 결정 |

**21번째 세션 결론**: F-04 는 D-day 차단 요소 아님 (WU-21 명시). 정규식/아키텍처 결정 모두 사용자 의존도 높아 자율 fix 불가. 사용자 복귀 시 위 2 항목 결정 후 별도 WU 로 분리 (WU-30 후보).

### 7.4 처리 방법 (✅ 22nd 세션 일괄 수행 완료)

22nd 세션 `adoring-trusting-feynman` 진입 시 사용자 §7 결정 **7항목 일괄 수신** + 적용:

1. ✅ 본 §7 항목 한 번에 검토 — 7항목 모두 사용자 답변 수신 (2026-04-25T22:55+09:00).
2. ✅ 7.1 (block) #1 = **(c) 별도 spec 파일** → `gates.md` 신설 완료 + 추가 메타 규칙 §1.14 (CLAUDE.md ≤200 lines) 신설.
3. ✅ 7.2 (product) #2-#6 = 5건 모두 사용자 결정 수신 → 본 §7.6 인덱스 표 참조 → 후속 WU 구현 시 inline 적용.
4. ✅ 7.5 (F-04 escalate) → **WU-30 신설로 분리** (정규식 (i) + 검증기 (b) 두 파일 분리).

상세 인덱스 = 본 §7.6.

### 7.6 22nd 세션 결정 인덱스 (사용자 일괄 수신 = 2026-04-25T22:55+09:00)

> 사용자 7항목 결정 → frontmatter `decision_points` WU22-D3 ~ WU22-D9 7건 reflective. 본 표는 사용자/AI 양쪽 빠른 참조용 dashboard.

| § | todo_id | 결정 | 영향 WU | 적용 위치 / 산출물 |
|:-:|:-------:|------|:------:|------|
| 7.1 #1 | WU22-D3 | (c) 별도 spec 파일 + 메타 규칙 §1.14 ≤200 lines | WU-25 + 본 세션 즉시 | `gates.md` (신설) + `CLAUDE.md §1.14` 신설 + `solon-status-report.md` (§14 분리) |
| 7.2 #2 | WU22-D4 | `·` 구분자 + 색상 on/off CLI flag (`--color=auto/always/never`) + ISO8601 | WU-24 | 추후 WU-24 §1 (`/sfs status`) 구현 시 |
| 7.2 #3 | WU22-D5 | (a) ISO week (`<YYYY-W>-sprint-<N>`) | WU-24 | 추후 WU-24 §1 (`/sfs start`) 구현 시 |
| 7.2 #4 | WU22-D6 | (b) 단순 파일 준비만 (no `$EDITOR` auto-launch) | WU-25, WU-26 | 추후 구현 시 |
| 7.2 #5 | WU22-D7 | (c) 하이브리드 ADR (frontmatter + 5섹션) | WU-29 | 추후 `/sfs decision` template 구현 시 |
| 7.2 #6 | WU22-D8 | (a) auto commit (push 는 §1.5 수동 유지) | WU-26 | 추후 `/sfs retro --close` 구현 시 |
| 7.5    | WU22-D9 | 7.5.1 = (i) `[A-Z]{2,}` minimal regex / 7.5.2 = (b) 두 검증기 분리 (`verify-install.sh` + `verify-w0.sh`) | **WU-30 신설** | 본 세션 step 6 = `sprints/WU-30.md` 신설 |

**Frontmatter `decision_points` 갱신** = 본 WU 의 frontmatter 에 WU22-D3 ~ WU22-D9 7건 추가 (resolved + resolution_session=adoring-trusting-feynman + impacts 명시).

**다음 entry**: 본 세션 step 6 = WU-30 (F-04 fix) 신설 → step 7 = WU-24 (#1 sfs slash 구현 part 1) entry → step 8 = 최종 PROGRESS.md.
